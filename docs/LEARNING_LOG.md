# 学习与开发记录

这个文件记录每个阶段学到的知识、做出的决定、验证结果，以及可以在面试中表达的经验。

## 记录模板

### 第 N 步：标题

- 日期：
- 目标：
- 学到的知识：
- 实际完成：
- 验证方式：
- 遇到的问题：
- 解决方法：
- 面试表达：
- 后续疑问：

## 第 0 步：建立项目路线

- 日期：2026-09-10
- 目标：把现有本地记账本逐步升级为可在线使用、可维护的全栈项目。
- 学到的知识：大型项目需要分阶段推进；每个阶段都应该有明确目标和验收标准。
- 实际完成：建立 `PROJECT_ROADMAP.md`，确定从前端工程化、本地数据层、后端、登录权限到部署的演进路线。
- 验证方式：确认路线图与当前项目状态一致，并保留现有功能不变。
- 遇到的问题：当前所有前端代码集中在一个 `index.html` 中，数据保存在浏览器 `localStorage`，暂时不适合多人在线使用。
- 解决方法：先进行工程化拆分，再逐步替换数据层，避免一次性重写导致无法理解或丢失数据。
- 面试表达：我先分析已有系统的边界，再设计分阶段迁移路线，每一步都保持可运行和可回退。
- 后续疑问：如何在拆分代码时保持现有 `localStorage` 数据兼容。

## 第 1 步：审查未提交的数据可靠性改动

- 日期：2026-09-11
- 目标：判断 `index.html` 中一批未提交修改是否应该保留或恢复。
- 学到的知识：恢复代码前要先看 Git diff，再做语法检查和针对性验证；“未提交”不等于“有问题”。
- 实际完成：审查金额按分保存、旧数据规范化、本地日期解析、统计修正、备份校验和导入失败恢复等改动。
- 验证方式：通过 `git diff --check` 检查格式问题，并提取内嵌 JavaScript 使用 Node.js 做语法检查，均通过。
- 遇到的问题：当前修改还没有完整的浏览器自动化测试，统计和备份功能仍需要手动场景验证。
- 解决方法：暂时保留修改，不直接恢复；下一步补充金额精度、退款报销、旧备份导入和导入失败恢复测试。
- 面试表达：我先通过差异审查定位风险，再用静态检查验证修改，最后根据风险决定保留，而不是直接回滚所有未提交代码。
- 后续疑问：如何为纯前端项目建立可重复的浏览器测试。

## 第 2 步：明确押金的统计口径

- 日期：2026-09-11
- 目标：保证押金不会被误算为收入或支出。
- 学到的知识：资金流向和损益统计是两个不同维度；押金会改变账户余额，但不代表赚到或花掉了钱。
- 实际完成：将四种押金方向统一为中性统计，只保留进入账户或离开账户的资金流向；押金仍会显示在明细中，但不进入收入、支出和统计图。
- 验证方式：检查 `incomeEffect`、`expenseEffect`、明细颜色和账户余额计算；JavaScript 语法检查和 Git 差异检查通过。
- 遇到的问题：原有逻辑把主动收押金归到收入侧、主动付押金归到支出侧，完整退回时再反向抵消，容易造成收入和支出统计失真。
- 解决方法：把押金定义为中性类型，单独使用 `flow` 字段计算账户余额。
- 面试表达：我区分了现金流和损益口径，避免把资产负债变动错误计入收入或支出。
- 后续疑问：是否需要在界面上单独增加“押金/往来”统计，展示当前仍未收回的押金。

## 第 3 步：增加押金对账和差额结算

- 日期：2026-09-11
- 目标：支持“付出 1000 元押金，最终只收回 700 元”的真实场景。
- 学到的知识：单独记录两笔资金流还不够，还需要用关联 ID 表达业务对应关系；最终差额属于损益，才应该进入收入或支出。
- 实际完成：退回押金时可以选择原始押金记录；勾选最终结算后，系统按原押金金额减去累计退回金额计算差额。
- 计算规则：付押金 1000、退回 700，账户余额净减少 300，支出统计增加 300；全额退回则差额为 0。
- 验证方式：通过 JavaScript 语法检查和 Git 差异检查；还需要在浏览器中完成完整操作验证。
- 遇到的问题：原数据结构没有押金之间的关联字段，无法判断 700 元退回对应哪一笔 1000 元押金。
- 解决方法：为退回记录增加 `depositLinkId` 和 `depositFinal`，保留原始资金流，同时只在最终结算记录上计算差额。
- 面试表达：我把押金建模为“资金流 + 业务关联 + 最终结算”，处理了部分退回和多次退回场景，避免把押金本金误计入损益。
- 后续疑问：需要补充浏览器测试，验证多次退回、删除原押金和编辑结算记录时的行为。

## 第 4 步：准备浏览器场景测试

- 日期：2026-09-11
- 目标：在真实浏览器中验证押金对账的新增、关联、最终结算和统计显示。
- 学到的知识：自动化测试不仅依赖代码，还依赖完整的运行工具链；需要先确认 Node.js、npm 和 npx。
- 实际完成：确认 Node.js 可用，但当前环境缺少 npm/npx；暂时保留静态检查结果并记录环境阻塞。
- 验证方式：Node.js 语法检查通过；浏览器交互测试待 npm/npx 可用后执行。
- 遇到的问题：无法启动 Playwright 命令行工具。
- 解决方法：先安装或补齐 npm 工具链，再执行浏览器场景测试。
- 面试表达：我能区分实现缺陷和测试环境缺失，先完成可执行的验证，再明确剩余验证条件。
- 后续疑问：安装 npm 后如何设计覆盖核心业务的最小浏览器测试集。

## 第 5 步：浏览器验证押金对账

- 日期：2026-09-11
- 目标：验证真实页面操作是否符合“付 1000、退 700、支出 300”的业务规则。
- 学到的知识：业务逻辑需要通过用户实际操作验证，静态语法正确不代表页面流程正确。
- 实际完成：在 Edge 浏览器中创建 1000 元原始押金，创建关联的 700 元退回记录并勾选最终结算。
- 验证结果：本月收入为 0，本月支出为 300，本月结余为 -300，净资产为 -300，符合预期。
- 遇到的问题：Playwright 默认依赖 Chrome，环境中没有 Chrome；改用已安装的 Edge 完成测试。首次加载页面的 favicon 产生 404，不影响业务功能。
- 解决方法：安装 Node.js/npm 工具链，使用 npx 启动 Playwright，并通过本地 HTTP 服务打开页面；测试完成后清除临时浏览器数据。
- 面试表达：我用端到端场景验证了核心财务规则，并检查了收入、支出、结余和净资产四个结果是否一致。
- 后续疑问：补充全额退回、多次退回和“收取押金后只退回部分”的测试。

### 手动验证结果

- 日期：2026-09-11
- 验证人：项目使用者
- 结果：手动测试押金对账流程，没有发现问题。
- 结论：当前押金对账规则和页面操作符合预期，可以进入下一阶段。

## 第 6 步：拆出独立样式文件

- 日期：2026-09-11
- 目标：在不改变页面表现和业务逻辑的前提下，把内嵌 CSS 从 `index.html` 移到独立文件。
- 学到的知识：HTML 负责结构，CSS 负责表现；通过相对路径引用样式文件可以降低入口文件复杂度。
- 实际完成：新增 `css/style.css`，将原有 `<style>` 内容完整迁移，并在 `index.html` 中加入样式表引用。
- 验证方式：Node.js 脚本语法检查通过；本地 HTTP 服务加载 `index.html` 返回 200；`css/style.css` 返回 200；页面快照显示主要控件正常渲染。
- 遇到的问题：直接打开本地文件不适合后续浏览器自动化测试，因此使用本地 HTTP 服务验证资源路径。
- 解决方法：使用相对路径 `css/style.css`，并通过本地服务确认浏览器能正确加载。
- 面试表达：我先把表现层从入口文件中分离出来，每次小范围重构后用浏览器验证资源加载和页面渲染，降低回归风险。
- 后续疑问：如何把脚本拆成模块，同时保证全局状态和事件绑定顺序不被破坏。

## 第 7 步：拆出数据存储模块

- 日期：2026-09-11
- 目标：把 `localStorage` 的读取、保存和旧数据迁移逻辑从入口脚本中分离出来。
- 学到的知识：传统脚本可以通过多个 `<script>` 按顺序加载；模块函数可以在运行时访问页面初始化的共享状态，但脚本顺序必须明确。
- 实际完成：新增 `js/storage.js`，迁移账目、账本、账户、自定义类型和自定义类别的读写函数；`index.html` 通过 `script src` 加载它。
- 验证方式：两个 JavaScript 文件语法检查通过；`js/storage.js` 返回 200；页面正常启动；新增 12 元支出后本月支出和净资产都正确显示。
- 遇到的问题：存储模块依赖入口脚本中的常量和状态变量，不能随意提前调用。
- 解决方法：只在页面初始化完成后调用存储函数，并保持 `storage.js` 在入口脚本前加载。
- 面试表达：我按照职责拆分持久化层，并通过真实页面操作验证重构没有破坏数据读写。
- 后续疑问：如何把共享状态进一步封装，减少模块对全局变量的依赖。

## 第 8 步：拆出备份模块

- 日期：2026-09-12
- 目标：把备份导出、导入、校验和导入后的页面刷新逻辑从入口脚本中分离出来。
- 学到的知识：备份属于数据交换功能，和日常 localStorage 读写有关联，但职责不同，应该单独组织。
- 实际完成：新增 `js/backup.js`，迁移备份提示、导出、导入、校验和 `reloadData` 函数；入口脚本按顺序加载 `storage.js`、`backup.js`，再加载页面逻辑。
- 验证方式：脚本语法检查通过；四个静态资源均返回 200；页面正常渲染；通过页面脚本确认 `exportBackup`、`importBackup`、`reloadData` 都仍是可调用函数。
- 遇到的问题：备份模块依赖页面初始化后的状态和渲染函数，不能在模块加载阶段立即执行。
- 解决方法：模块只声明函数，把实际调用留给页面事件和导入流程。
- 面试表达：我按数据职责拆分持久化和备份模块，并保持脚本加载顺序与运行时依赖清晰。
- 后续疑问：如何把记录、账户和押金业务逻辑继续拆分，同时减少对全局变量的依赖。

## 第 9 步：拆出记账计算模块

- 日期：2026-09-12
- 目标：把收入、支出、押金结算、资金方向和账户余额计算从入口脚本中分离出来。
- 学到的知识：计算逻辑应该和页面渲染分开；同一组业务计算可以被明细、统计卡片、图表和账户余额共同使用。
- 实际完成：新增 `js/records.js`，迁移收入/支出判断、金额显示、资金流向、账户余额和押金差额结算函数。
- 验证方式：脚本语法检查通过；页面和五个静态资源均正常加载；直接验证“原押金 1000、退回 700”得到收入 0、支出 300、余额 -300。
- 遇到的问题：计算函数依赖 `records`、`accounts`、自定义类型和押金配置等共享状态。
- 解决方法：先保持现有全局状态不变，只分离函数职责；后续再引入统一状态对象，减少隐式依赖。
- 面试表达：我先把稳定的领域计算抽出来，保持对外函数行为不变，再逐步降低模块对页面代码的依赖。
- 后续疑问：如何拆分记账表单的新增、编辑、删除事件，同时避免事件绑定散落在入口文件中。

## 第 10 步：拆出记账表单模块

- 日期：2026-09-12
- 目标：把新增、编辑、取消和删除记录的交互逻辑从入口脚本中分离出来。
- 学到的知识：表单模块负责收集和校验用户输入、更新记录状态并触发保存；计算模块负责解释记录的业务含义，两者应该分开。
- 实际完成：新增 `js/record-form.js`，迁移 `handleSave`、`startEdit`、`cancelEdit`、`deleteRec`、`resetForm` 和提示消息逻辑。
- 验证方式：模块语法检查通过；页面正常加载；通过浏览器新增 12 元支出，页面正确显示本月支出和净资产；表单处理函数均可调用。
- 遇到的问题：表单处理依赖页面状态、计算函数、存储函数和渲染函数，模块之间存在明确的调用顺序。
- 解决方法：在入口脚本之前加载模块，只在用户操作或页面初始化之后调用函数，暂时保持共享状态兼容。
- 面试表达：我把表单交互和领域计算分离，通过保持函数接口不变完成渐进式重构，并用端到端操作验证保存流程。
- 后续疑问：如何把账户管理和账本管理也拆分，并逐步消除全局状态依赖。

## 第 11 步：拆出账户表单模块

- 日期：2026-09-12
- 目标：把账户的新增、编辑、删除和表单提示逻辑从入口脚本中分离出来。
- 学到的知识：账户管理属于独立的表单交互职责；拆分时要保留原有函数名和页面状态约定，才能降低重构风险。
- 实际完成：新增 `js/account-form.js`，迁移 `showAccMsg`、`resetAccountForm`、`handleAccountSave`、`startEditAccount` 和 `deleteAccount`，并在 `index.html` 中按依赖顺序加载。
- 验证方式：在 Edge 浏览器中新增“测试账户”并设置本金 100 元，确认净资产变为 100 元；编辑为“测试账户改”后列表更新；删除后确认账户从列表消失；测试数据随后清除。模块语法检查、资源加载和 Git 差异格式检查均通过。
- 遇到的问题：账户表单仍需要调用共享的 `accounts`、`records`、存储函数和渲染函数，不能把它当成完全独立的组件。
- 解决方法：先只分离账户表单的行为，保持共享状态和原有函数接口不变；等后续建立统一状态层后，再进一步减少模块之间的隐式依赖。
- 面试表达：我采用渐进式重构，先按职责拆出账户管理模块，再用新增、编辑、删除三个真实用户流程验证行为没有回归。
- 后续疑问：如何拆分账本管理和页面渲染，同时让各模块通过明确接口协作。

## 第 12 步：拆出账本管理模块

- 日期：2026-09-12
- 目标：把账本切换、账本列表、新增、编辑、删除和管理面板显示逻辑从入口脚本中分离出来。
- 学到的知识：一个功能模块可以同时包含状态切换和表单操作，但仍要把存储职责留给存储层，把页面总刷新留给已有渲染函数。
- 实际完成：新增 `js/book-form.js`，迁移 `showBookMsg`、`renderBookSelect`、`switchBook`、`renderBookList`、`resetBookForm`、`handleBookSave`、`startEditBook`、`deleteBook` 和 `toggleBookPanel`，并加入入口脚本之前的加载顺序。
- 验证方式：在 Edge 浏览器中新增“测试账本”并设置“学习”分类，确认顶部下拉出现；切换后确认该账本被选中；编辑为“测试账本改 / 项目”后确认列表和下拉同步更新；删除后确认自动回到“我的账本”，并确认临时账本消失。模块语法检查、静态资源加载和 Git 差异格式检查均通过。
- 遇到的问题：切换或删除当前账本时，需要同时重新读取账目、账户、筛选条件和表单状态，账本模块与页面状态存在多个协作点。
- 解决方法：保留原有切换流程和函数接口，只移动账本相关代码；删除测试账本时使用针对该账本 ID 的数据清理，避免影响其他账本。
- 面试表达：我按业务边界拆分多账本管理功能，覆盖新增、切换、编辑、删除和当前账本回退场景，并用浏览器流程验证数据隔离和页面同步。
- 后续疑问：如何把页面渲染函数集中到独立的视图模块，并通过统一状态对象减少跨模块的全局变量依赖。

## 第 13 步：拆出基础页面视图模块

- 日期：2026-09-12
- 目标：把统计卡片、阶段汇总、明细列表、账户列表、页面刷新和标签页切换从入口脚本中分离出来。
- 学到的知识：视图层主要负责把状态转换成 DOM；业务计算应继续由 `records.js` 提供，数据读写也不应重新混入视图模块。
- 实际完成：新增 `js/view.js`，迁移 `renderStats`、`renderPeriodSummary`、`renderList`、`renderAccountList`、`refreshDetail`、`render` 和 `switchTab`，并在入口脚本中按顺序加载。
- 验证方式：在 Edge 浏览器中新增 12 元餐饮支出，确认本月支出、结余和净资产均为 -12 元；切换到明细页并展开列表，确认记录和统计图正常显示；切换到账户页，确认现金余额为 -12 元；删除临时记录后确认各项恢复为 0。所有脚本语法检查、入口内嵌脚本检查、资源加载和 Git 差异格式检查均通过。
- 遇到的问题：视图函数仍需要访问全局状态和其他模块函数，完全改成独立组件需要先定义统一的数据接口。
- 解决方法：本阶段只移动 DOM 渲染职责，保留原有函数名和调用关系；下一阶段再拆分统计图和总览，并逐步设计状态接口。
- 面试表达：我按“计算、存储、视图”分层进行渐进式重构，每次迁移后用真实浏览器流程验证关键指标和页面切换没有回归。
- 后续疑问：如何把统计图和跨账本总览从入口文件中分离，并减少视图层对全局变量的直接依赖。

## 第 14 步：拆出统计图模块

- 日期：2026-09-12
- 目标：把收入、支出、综合三种统计口径，以及柱形图和扇形图的绘制逻辑从入口脚本中分离出来。
- 学到的知识：图表模块先把记录按业务口径聚合，再把聚合结果转换成 SVG；图表交互状态和页面其他筛选状态需要通过明确函数协作。
- 实际完成：新增 `js/charts.js`，迁移 `renderChartKindChips`、`renderChartTypeChips`、`renderChart`、综合图和普通柱形/扇形图绘制函数，并在入口脚本中按顺序加载。
- 验证方式：在 Edge 浏览器中创建 12 元支出和 30 元收入，确认统计卡片为收入 30、支出 12、结余 18；切换收入口径、扇形图、综合口径和柱形图，确认图表和图例同步更新；删除两笔临时记录后确认数据恢复为 0。
- 遇到的问题：图表模块依赖 `filteredRecords`、收入/支出计算函数和配色常量，不能在没有页面状态的情况下单独启动。
- 解决方法：本阶段保持现有全局状态和函数接口，只移动图表生成职责；后续再通过统一状态对象和纯数据聚合函数进一步解耦。
- 面试表达：我把图表交互和数据计算分层，覆盖多种统计口径与图形切换，并用收入、支出混合场景验证可视化结果。
- 后续疑问：如何把跨账本总览和读取其他账本数据的逻辑独立出来，同时保证当前账本切换后总览及时刷新。

## 第 15 步：拆出跨账本总览模块

- 日期：2026-09-12
- 目标：把读取其他账本数据、计算各账本净资产和显示本月收支的逻辑从入口脚本中分离出来。
- 学到的知识：跨账本查询不能直接改动当前账本的内存状态；读取时使用指定账本 ID，计算完成后再交给视图输出。
- 实际完成：新增 `js/overview.js`，迁移 `readBookRecords`、`readBookAccounts` 和 `renderOverview`，并在入口脚本中按依赖顺序加载。
- 验证方式：在两个账本中分别创建 12 元支出和 30 元收入；总览正确显示两个账本的本月收支、各自净资产，以及全部账本合计净资产 18 元；删除临时账目和测试账本后，当前账本恢复为原始状态。
- 遇到的问题：总览需要读取其他账本的数据格式，同时复用押金、账户余额和金额规范化逻辑，不能另写一套计算规则。
- 解决方法：复用 `storage.js` 的键名和规范化函数、`records.js` 的余额与收支计算，只让总览模块负责跨账本遍历和 DOM 展示。
- 面试表达：我实现了只读的跨账本汇总，保持当前账本状态不被污染，并通过两个账本的混合收支场景验证合计结果。
- 后续疑问：如何把入口脚本中的事件绑定和初始化流程也模块化，让启动顺序更加清晰。

## 第 16 步：拆出应用启动模块

- 日期：2026-09-12
- 目标：把按钮事件绑定、输入事件绑定和首次数据加载从 `index.html` 的内嵌脚本中分离出来。
- 学到的知识：启动脚本必须在页面结构、状态变量和功能函数都准备好之后执行；脚本标签的位置本身就是依赖关系的一部分。
- 实际完成：新增 `js/app.js`，迁移记账、筛选、账户、账本、备份等事件绑定，以及 `loadBooks`、`load`、`loadAccounts` 和首次 `render` 调用；入口文件在内嵌脚本结束后加载它。
- 验证方式：Edge 浏览器加载页面正常；账户、明细、总览和记一笔四个标签页均可切换；新增 5 元支出后统计卡片正确显示；`exportBackup` 事件处理函数可用；删除临时记录后数据恢复为 0。所有脚本、入口脚本、资源加载和 Git 格式检查均通过。
- 遇到的问题：如果把启动脚本和功能模块一起提前加载，可能在状态变量和 DOM 尚未准备好时执行，导致初始化失败。
- 解决方法：让 `app.js` 作为最后一个脚本加载，明确区分“声明功能”和“执行启动”两个阶段。
- 面试表达：我把应用启动过程独立出来，通过明确脚本顺序解决初始化依赖，并用多页面切换和真实保存流程验证启动行为。
- 后续疑问：如何继续减少内嵌脚本，逐步把状态定义和筛选逻辑也组织成更清晰的模块接口。

## 第 17 步：拆出筛选模块并修正筛选刷新

- 日期：2026-09-12
- 目标：统一处理日期、账户、类别和关键字筛选，并保证筛选结果同步影响明细汇总和统计图。
- 学到的知识：筛选是数据查询层职责；当一个条件影响多个视图时，事件处理应调用统一刷新入口，而不是只更新其中一个区域。
- 实际完成：新增 `js/filters.js`，迁移 `filterByPeriod`、`matchesSearch`、`filteredRecords` 和负债账户判断；同时把关键字输入事件从只刷新明细改为调用 `refreshDetail`。
- 验证方式：创建 5 元餐饮支出和 20 元交通支出；输入关键字“早餐”后，汇总变为支出 5 元、图表只显示餐饮；切换类别“交通”后，汇总变为支出 20 元、图表只显示交通；清空筛选并删除两笔临时记录后，统计恢复为 0。
- 遇到的问题：首次自动化操作使用了旧页面引用，导致第二笔记录的类型和类别按钮没有点击；另外发现关键字筛选只更新列表而没有更新汇总和图表。
- 解决方法：每次页面重绘后重新获取控件引用；把搜索事件接入统一的 `refreshDetail` 流程，确保列表、汇总和图表使用同一组筛选结果。
- 面试表达：我将多条件筛选抽成独立模块，并通过发现和修复“列表与汇总不一致”的问题，保证同一查询条件下多个视图结果一致。
- 后续疑问：如何把状态变量集中管理，进一步减少筛选、视图和表单模块之间的全局依赖。

## 第 18 步：拆出自定义类型与类别模块

- 日期：2026-09-12
- 目标：把自定义类型、自定义类别的渲染、添加和删除逻辑从入口脚本中分离出来。
- 学到的知识：模块可以先按用户操作边界拆分；只要保留稳定的函数接口，就能在不改业务规则的情况下渐进式重构。
- 实际完成：新增 `js/custom-options.js`，迁移类型/类别按钮渲染、自定义输入框、收入/支出/中性选择、重复校验和删除确认；入口文件按依赖顺序加载该模块。
- 验证方式：在 Edge 中新增“临时类型”（算支出）和“临时类别”，刷新页面后展开列表确认两项仍可选；分别通过删除按钮和确认框删除，页面恢复为内置选项。
- 遇到的问题：刷新后自定义类型默认处于折叠区域之外，第一次使用旧引用点击展开失败。
- 解决方法：重新获取刷新后的页面引用，再展开类型列表；确认自定义类型出现在展开列表中，并清理测试数据。
- 面试表达：我按用户操作边界拆分自定义选项模块，覆盖持久化、重复校验、删除确认和刷新恢复场景，同时保持原有函数接口不变。
- 后续疑问：如何把散落在各模块中的状态变量集中管理，减少隐式的全局依赖。

## 第 19 步：集中管理静态配置

- 日期：2026-09-12
- 目标：把 localStorage 键名、内置类型/类别、押金方向、图表配色、默认账户等不会随一次操作变化的配置从入口脚本中移出。
- 学到的知识：配置和运行时状态的生命周期不同；配置适合集中维护，状态仍需要在应用启动时初始化和更新。
- 实际完成：新增 `js/constants.js`，迁移键名、账本键名生成函数、流水类型、押金方向、颜色、折叠数量和默认账户；入口文件在所有业务模块之前加载它。
- 验证方式：Edge 页面正常加载并显示 7 个默认账户；新增 1.23 元餐饮支出后，收入为 0、支出为 1.23、净资产为 -1.23；进入明细确认记录和图表可见，随后删除临时记录并恢复为 0。
- 遇到的问题：浏览器默认启动器找不到 Chrome，且当前终端没有把 npm/npx 放进 PATH。
- 解决方法：确认机器已有完整 Node.js 路径和 Microsoft Edge，使用 Edge 完成回归；代码本身仍先通过 Node 语法检查。
- 面试表达：我把稳定配置与运行时状态分离，明确脚本加载顺序，并通过金额、账户和图表流程验证配置迁移没有改变业务行为。
- 后续疑问：如何把散落在各模块中的状态变量集中管理，减少隐式的全局依赖。

## 第 20 步：集中管理筛选运行时状态

- 日期：2026-09-12
- 目标：把日期、账户、类别和关键字筛选值从多个全局变量集中到一个状态对象中。
- 学到的知识：状态对象负责保存数据，事件模块负责更新状态，视图模块负责读取状态；把三者职责分开可以降低变量散落带来的维护成本。
- 实际完成：新增 `js/state.js`，提供 `filterState` 和 `resetFilterState()`；筛选、备份、账本切换和自定义类别删除逻辑改为读写该对象，入口脚本中的旧筛选变量已移除。
- 验证方式：新增 10.50 元餐饮支出并在明细页搜索“状态测试”，汇总仍显示支出 10.50 元；切换到临时账本后搜索框、日期和下拉筛选均恢复为空；删除临时账本和记录后主账本恢复为 0。
- 遇到的问题：切换账本会重绘页面，旧的筛选控件引用会失效；如果只清理内存值而不清理输入框，用户界面会与状态不一致。
- 解决方法：用 `resetFilterState()` 统一清理对象，再由账本模块同步清空对应控件；每次重绘后重新获取浏览器控件引用。
- 面试表达：我采用渐进式状态重构，先迁移耦合最明显的筛选状态，并用跨账本切换验证状态、控件和汇总结果保持一致。
- 后续疑问：如何继续集中管理表单编辑状态和当前标签页，同时避免一次性改动过大。

## 第 21 步：集中管理界面运行时状态

- 日期：2026-09-12
- 目标：把编辑记录、当前标签页、明细展开、类型/类别展开和自定义类型口径等界面状态集中起来。
- 学到的知识：界面状态和筛选状态都属于运行时数据；按职责放进状态对象，可以减少模块之间对同名全局变量的依赖。
- 实际完成：扩展 `js/state.js` 的 `uiState`，并迁移 `editingRecordId`、`activeTab`、`detailExpanded`、`typesExpanded`、`categoriesExpanded`、`customTypeSideDraft`；表单、视图、账本和自定义选项模块完成适配。
- 验证方式：新增 12.34 元餐饮支出；切换明细页并展开列表，进入编辑后确认金额、账户和备注正确回填；点击取消后表单恢复“保存这笔账”，统计仍显示 12.34 元；删除临时记录后恢复为 0。
- 遇到的问题：编辑流程会重绘类型和类别按钮，旧的 DOM 引用会失效；取消编辑还要同时清空编辑 ID 和表单输入。
- 解决方法：让 `uiState.editingRecordId` 作为编辑流程唯一来源，重绘后重新获取控件引用，继续复用 `resetForm()` 清理表单。
- 面试表达：我把筛选状态和界面状态分开建模，先迁移编辑与导航状态，并用新增、展开、编辑、取消、删除的完整流程验证行为没有回归。
- 后续疑问：如何把当前账本和账户数据也放入统一状态边界，同时保持存储层的职责清晰。

## 第 22 步：集中管理当前账本状态

- 日期：2026-09-12
- 目标：把当前打开的账本 ID 从入口脚本移到数据状态边界，统一存储读写、账本切换和总览标记。
- 学到的知识：当前账本 ID 是跨模块共享的数据上下文；把它集中管理后，localStorage 键名、页面显示和切换流程可以使用同一个来源。
- 实际完成：在 `js/state.js` 增加 `dataState.currentBookId`，并迁移 `storage.js`、`book-form.js`、`overview.js` 中的当前账本读写；入口脚本的旧变量已移除。
- 验证方式：Edge 中创建“当前账本测试”，切换后顶部下拉显示该账本且主页面数据为空；删除当前临时账本后自动回到“我的账本”，主账本统计保持 0。
- 遇到的问题：删除当前账本时既要更新状态对象，也要更新 localStorage 和页面下拉，否则刷新后可能回到已删除的 ID。
- 解决方法：在删除流程中先把 `dataState.currentBookId` 回退到剩余账本，再持久化并重新加载账目、账户和页面。
- 面试表达：我把当前账本作为共享数据上下文集中管理，并验证了创建、切换、删除当前账本和自动回退的完整流程。
- 后续疑问：如何把账目和账户数组迁移到数据状态对象，同时避免大范围替换造成回归。

## 第 23 步：集中管理账目数据

- 日期：2026-09-12
- 目标：把当前账本的账目数组从入口脚本移到 `dataState.records`，让存储、计算、筛选、表单和视图使用同一个数据来源。
- 学到的知识：数据状态应有明确的所有权；数组从入口变量迁移到状态对象后，新增、编辑、删除和重新加载都能沿着同一条路径流动。
- 实际完成：在 `js/state.js` 增加 `records`；迁移 `storage.js`、`records.js`、`filters.js`、`record-form.js`、`view.js`、自定义选项和入口押金逻辑中的账目引用。
- 验证方式：新增 7.89 元餐饮支出，确认首页支出、净资产、明细和图表正确；刷新后统计仍为 -7.89 元，说明从 localStorage 的读取仍有效；删除临时记录后恢复为 0。
- 遇到的问题：账目数组同时被存储、业务计算和页面渲染使用，直接改名容易遗漏引用。
- 解决方法：先用全文搜索列出所有 `records` 读写点，再按存储、计算、筛选、表单、视图顺序逐个替换，最后用新增、刷新、删除流程验证。
- 面试表达：我为账目数据建立了明确状态边界，并通过静态引用检查和浏览器回归验证迁移没有改变持久化与统计行为。
- 后续疑问：如何用同样方式迁移账户数组，并确保余额计算和账户管理仍然一致。

## 第 24 步：集中管理账户数据

- 日期：2026-09-12
- 目标：把当前账本的账户数组从入口脚本移到 `dataState.accounts`，统一账户管理、下拉选项、负债判断和余额计算的数据来源。
- 学到的知识：账户数据既影响表单选项，也影响领域计算；迁移时必须同时覆盖“显示账户”和“计算余额”两条路径。
- 实际完成：在 `js/state.js` 增加 `accounts`；迁移 `storage.js`、`records.js`、`filters.js`、`account-form.js`、`view.js` 和入口账户下拉渲染中的账户引用。
- 验证方式：Edge 中新增“状态账户”，本金 50 元，净资产变为 50 元；记账页账户下拉出现该账户；删除账户后净资产恢复为 0，默认账户列表保持完整。
- 遇到的问题：账户数组被余额计算和账户管理同时使用，只迁移其中一侧会造成列表与统计不一致。
- 解决方法：先全文列出所有账户读写点，再同时替换读取、添加、编辑、删除、保存和余额计算路径，最后用新增账户和删除账户流程验证。
- 面试表达：我把账户数据纳入统一状态边界，覆盖本金、账户选择、余额计算和删除回退场景，避免页面显示与业务计算使用不同数据源。
- 后续疑问：如何继续迁移编辑账户、编辑账本等剩余状态，并逐步让模块通过明确接口协作。

## 第 25 步：集中管理账户与账本编辑状态

- 日期：2026-09-12
- 目标：把账户和账本表单的编辑 ID 从入口脚本移到 `uiState`，明确区分“新增”和“修改”两条保存路径。
- 学到的知识：编辑上下文属于界面运行时状态；用一个显式 ID 表示当前编辑对象，可以避免保存时误走新增分支，也方便取消操作统一清理。
- 实际完成：在 `js/state.js` 增加 `uiState.editingAccountId` 和 `uiState.editingBookId`；`account-form.js`、`book-form.js` 的进入编辑、保存、取消和重置流程统一读写这两个字段，入口脚本中的旧变量已移除。
- 验证方式：Edge 中新增临时账户并修改名称、本金，确认净资产从 10 元更新为 20 元；再次编辑后输入无效值并取消，确认原数据保持不变；随后删除临时账户。新增临时账本后修改名称、分类并保存，再次编辑后取消，确认列表没有重复项；删除临时账本后仅保留主账本。
- 遇到的问题：表单保存或取消后页面会重绘，之前保存的按钮引用会失效；如果只清空输入框而不清理编辑 ID，下一次新增可能仍被当成修改。
- 解决方法：每次重绘后重新获取页面控件；在 `resetAccountForm()` 和 `resetBookForm()` 中统一把对应编辑 ID 设回 `null`，并根据 ID 是否存在决定按钮文字和保存分支。
- 面试表达：我把账户和账本的编辑上下文纳入 `uiState`，通过新增、保存修改、取消修改和删除的端到端流程验证没有产生重复数据。
- 后续疑问：如何把 `books` 数组和剩余的选择状态继续迁移到 `dataState`，让入口脚本只负责启动应用。

## 第 26 步：集中管理账本、选项和图表状态

- 日期：2026-09-12
- 目标：把账本清单、自定义类型/类别等业务数据放入 `dataState`，把记账选择和图表选择放入 `uiState`，进一步减少入口脚本中的独立状态变量。
- 学到的知识：状态对象可以按生命周期区分数据和界面选择；业务数据需要持久化，临时选择只在当前页面会话中存在，两者不应混在同一层。
- 实际完成：在 `js/state.js` 增加 `dataState.books`、`customTypes`、`customCategories`，以及 `uiState.selectedType`、`selectedCategory`、`selectedDepositDir`、`chartType`、`chartKind`；迁移存储、账本、总览、记账表单、自定义选项和图表模块，入口脚本中的旧变量已移除。
- 验证方式：Edge 中新增并切换临时账本，在其中保存 2.34 元餐饮支出和 1.11 元自定义类别支出；明细汇总显示支出 3.45 元，综合/扇形图切换后图例正确；自定义类别刷新后仍能出现在筛选下拉，自定义类型按“算支出”显示；删除自定义选项和临时账本后主账本净资产回到 0。
- 遇到的问题：新增自定义类别后，按钮列表已更新，但明细类别筛选下拉仍使用旧 DOM 内容；状态已经变化，视图却没有同步。
- 解决方法：在自定义类别新增分支调用 `fillCategoryFilter()`，让同一个状态变化同时刷新按钮和筛选下拉；页面刷新后再验证 localStorage 恢复路径。
- 面试表达：我按“持久化业务数据”和“会话界面状态”划分状态边界，并修复了新增自定义类别后列表与筛选下拉不同步的问题。
- 后续疑问：如何把入口脚本中的工具函数和押金交互也拆成明确接口，最终让入口只保留启动编排。

## 第 27 步：拆出通用工具模块

- 日期：2026-09-12
- 目标：把金额换算、旧数据规范化、日期处理、HTML 转义、ID 生成和类型口径判断从 `index.html` 移到独立工具模块。
- 学到的知识：被多个模块依赖的纯函数应该有稳定的共享位置；脚本拆分不仅是移动代码，还要同时维护加载顺序和依赖边界。
- 实际完成：新增 `js/helpers.js`，迁移 `toCents`、`amountOf`、记录/账户规范化、`nextId`、日期函数、`esc`、类型/类别查询和账户需求判断；入口脚本只保留账户下拉和押金交互，`helpers.js` 在状态模块之后、存储模块之前加载。
- 验证方式：Edge 中新增 3.21 元餐饮支出，首页结余、净资产和明细均显示 -3.21；展开明细确认日期、账户和备注正确；删除测试记录后统计恢复为 0。页面初始化、脚本语法、入口内嵌脚本和 Git 差异检查均通过。
- 遇到的问题：工具函数依赖 `constants.js` 和 `state.js` 的数据，但存储模块又依赖工具函数，加载顺序错误会导致页面启动失败。
- 解决方法：按“常量 → 状态 → 工具 → 存储 → 业务模块 → 启动”的顺序排列脚本；工具函数只读取状态，不在加载阶段执行 DOM 操作。
- 面试表达：我提取了跨模块复用的纯函数，并通过金额、日期、旧数据兼容和页面回归验证拆分没有改变业务结果。
- 后续疑问：如何把押金方向、关联原押金和最终结算提示从入口脚本拆成独立交互模块。

## 第 28 步：拆出押金交互模块

- 日期：2026-09-12
- 目标：把押金方向按钮、原押金关联、最终结算提示和相关字段显隐从 `index.html` 移到独立模块，同时保持押金本金中性、最终差额计入收支的规则。
- 学到的知识：页面交互模块可以负责状态和 DOM 协作，领域计算仍由计算模块提供；拆分时要保留模块之间清晰的函数接口。
- 实际完成：新增 `js/deposit-form.js`，迁移 `updateFormFields`、押金方向渲染、对应原押金查询、关联下拉和差额提示；脚本按“常量 → 状态 → 工具 → 押金交互 → 存储 → 业务模块 → 启动”加载。
- 验证方式：Edge 中录入主动支付 1000 元押金，确认收入和支出均为 0、净资产为 -1000；再录入关联退回 700 元，勾选最终结算，提示显示差额 300，保存后支出为 300、净资产为 -300；删除两笔临时记录后恢复为 0。
- 遇到的问题：第一次保存退回押金时忘记选择账户，表单按规则阻止保存；补选账户后流程正常。
- 解决方法：保留 `needsAccount` 对押金账户的校验，并在回归测试中把“字段显隐、原押金关联、账户选择、最终结算”分别检查。
- 面试表达：我把押金交互从入口脚本中独立出来，复用统一的差额计算规则，并用 1000 元支付、700 元退回的真实场景验证中性本金和最终差额没有混淆。
- 后续疑问：如何把账户和筛选下拉等剩余 DOM 辅助函数也移出入口，让入口脚本只保留初始化编排。

## 第 29 步：拆出账户与筛选下拉模块

- 日期：2026-09-12
- 目标：把账户选项、账户筛选和类别筛选的 DOM 生成从 `index.html` 移到独立模块。
- 学到的知识：下拉渲染属于界面适配层；它把 `dataState` 转成 DOM 选项，不应该混入余额计算或存储逻辑。
- 实际完成：新增 `js/selects.js`，迁移 `fillAccountSelect`、`renderAccountSelects`、`fillAccountFilter`、`fillCategoryFilter`；脚本在工具和押金交互之后、存储模块之前加载。
- 验证方式：Edge 中新增本金 12.34 元的临时账户，确认记账页账户下拉和明细页账户筛选都出现；新增临时类别后明细类别筛选立即出现；删除测试账户和类别后净资产回到 0。
- 遇到的问题：下拉选项是由 DOM 生成的，账户或类别状态变化后，旧选项不会自动更新；只更新 `dataState` 会导致页面不同区域显示不一致。
- 解决方法：集中使用 `fillAccountFilter()`、`fillCategoryFilter()` 刷新筛选入口，并让 `renderAccountSelects()` 一次更新记账页和筛选页的账户下拉。
- 面试表达：我把 `dataState` 到 DOM 的适配逻辑独立出来，统一刷新入口，避免账户列表、记账表单和明细筛选使用不同选项。
- 后续疑问：如何把入口脚本中剩余的交互绑定和初始化流程继续收敛成更明确的应用接口。

## 第 30 步：增加本地存储适配层

- 日期：2026-09-12
- 目标：避免业务模块直接依赖 `localStorage`，为以后切换 IndexedDB 或后端 API 保留替换边界。
- 学到的知识：适配层的价值不是增加复杂封装，而是把外部依赖集中到一个稳定接口；业务模块只关心“读取、保存、删除”和批量数据，不关心具体存储实现。
- 实际完成：新增 `js/storage-adapter.js`，提供 `appStorage.get/set/remove/entries/removeMany/setMany`；迁移 `storage.js`、`backup.js`、`book-form.js`、`overview.js` 的直接存储调用，入口按“工具、下拉、适配层、存储”顺序加载。
- 验证方式：Edge 中新增 4.56 元餐饮支出，确认首页支出和净资产为 -4.56；刷新页面后明细仍能读到该记录；导出备份成功生成带 `format`、`version` 和存储键的 JSON；删除测试记录后统计恢复为 0。
- 遇到的问题：备份导入/导出需要遍历多个键，若每个模块各自遍历 `localStorage`，以后更换存储实现会遗漏批量操作。
- 解决方法：在适配层统一提供按前缀读取、批量删除和批量写入，备份模块只处理备份格式和回滚顺序；其他模块不再直接访问 `localStorage`。
- 面试表达：我用适配层隔离浏览器存储细节，迁移过程中保持旧键名和备份格式不变，并用新增、刷新、导出和删除流程验证数据兼容性。
- 后续疑问：如何给存储层增加数据版本和迁移机制，让旧版本数据能够安全升级。

## 第 31 步：增加数据版本和旧数据迁移机制

- 日期：2026-09-12
- 目标：让应用能识别存储结构版本，并把最早的单账本数据安全迁移到现在的分账本结构。
- 学到的知识：迁移要有明确版本、可重复执行的步骤和完成标记；迁移成功后才提升版本号，页面中断后再次打开可以继续处理。
- 实际完成：新增 `js/migrations.js`，加入 `STORAGE_SCHEMA_KEY` 和 `STORAGE_SCHEMA_VERSION`；v1 迁移会把旧的 `jizhangben_records`、`jizhangben_accounts` 移到默认账本键，并删除旧键；`loadBooks()` 在读取账本前调用 `runMigrations()`。
- 验证方式：用合法 JSON 模拟旧版记录和账户，刷新页面后首页显示支出 3.21 元；旧键被移除，新账本键和 schema 版本 `1` 均生成；删除模拟数据后页面恢复为 0 元空账本。
- 遇到的问题：第一次用浏览器命令模拟旧数据时，命令行转义去掉了 JSON 引号，导致迁移后的内容无法解析，页面统计显示为 0。
- 解决方法：先检查迁移前后的原始存储值，确认是测试输入问题后，改用页面内 `JSON.stringify` 写入合法 JSON；清理时只删除本次生成的明确键，避免无差别清除数据。
- 面试表达：我为本地数据增加版本标记和幂等迁移流程，兼容旧版单账本键，并用真实浏览器验证迁移结果和失败排查过程。
- 后续疑问：如何把不同版本的记录字段变化也纳入迁移，并为迁移失败提供更明确的用户提示和回滚策略。

## 第 32 步：迁移旧字段并持久化金额分值

- 日期：2026-09-13
- 目标：把读取时临时补齐的金额字段正式写回存储，让记录和账户数据在 schema v2 下拥有统一结构。
- 学到的知识：数据迁移不仅可以搬动键名，也可以升级字段；迁移前要区分可解析数据和损坏数据，不能用空结果覆盖原始内容。
- 实际完成：将 `STORAGE_SCHEMA_VERSION` 提升为 `2`；在 `js/migrations.js` 增加 v2 迁移，遍历所有分账本记录和账户，分别调用 `normalizeRecords()`、`normalizeAccounts()` 写回 `amountCents` 和 `initialCents`；无法解析的键保持原样，交给读取层兜底。
- 验证方式：把 schema 临时设为 `1`，写入 6.789 元记录和 10.239 元账户本金；刷新后页面显示 6.79 元支出、净资产 3.45 元，存储中分别出现 `amountCents: 679` 和 `initialCents: 1024`，版本升为 `2`；删除测试键后恢复为 0。
- 遇到的问题：迁移测试依赖浏览器本地数据，直接清空存储可能误删其他账本，且旧字段金额会受到小数精度影响。
- 解决方法：只修改当前测试账本的 schema 和明确键，使用整数分值规范化并在页面和原始存储两侧同时核对结果。
- 面试表达：我在保持用户可见金额不变的前提下，把兼容逻辑升级为持久化字段迁移，并对损坏数据采用跳过策略，降低迁移覆盖风险。
- 后续疑问：如何为迁移过程增加事务式回滚和用户可读的失败提示。

## 第 33 步：增加迁移快照回滚和失败提示

- 日期：2026-09-13
- 目标：迁移中任何写入失败时恢复升级前的本地数据，并把结果用用户能理解的方式显示在页面上。
- 学到的知识：迁移的版本号、数据写入和删除必须被视为一个整体；捕获异常的位置会决定错误能否传播到统一回滚层。
- 实际完成：`runMigrations()` 在升级前保存所有记账本键的快照，失败后移除迁移新增的键并恢复原值；回滚成功和回滚失败使用不同提示；新增页面顶部警告区域，由 `uiState.startupNotice` 和 `renderStartupNotice()` 控制。
- 验证方式：在独立浏览器站点模拟账户数据写入失败，确认函数返回失败、schema 保持 `1`、已被修改的记录恢复为旧字段、页面显示“已恢复升级前的数据”；解除故障并刷新后迁移成功，schema 升到 `2`，警告自动隐藏。
- 遇到的问题：最初 v2 迁移的 `catch` 同时捕获了解析错误和写入错误，导致写入失败被当作普通损坏数据跳过，版本仍错误地升到 `2`。
- 解决方法：把 `try/catch` 缩小到 `JSON.parse`，只跳过无法解析的数据；让 `appStorage.set()` 的异常继续向上传递，由 `runMigrations()` 统一回滚。
- 面试表达：我通过故障注入发现迁移异常被局部吞掉的问题，调整异常边界并实现快照回滚，同时验证失败提示、原值恢复和再次升级三个结果。
- 后续疑问：如何为迁移和关键金额计算增加可重复运行的自动化测试，而不依赖手动构造浏览器数据。

## 第 34 步：建立第一组自动化回归测试

- 日期：2026-09-13
- 目标：把金额规范化和数据迁移的关键规则变成可以反复运行的自动检查，减少每次修改后完全依赖手动测试的风险。
- 学到的知识：单元测试适合验证输入和输出明确的函数；测试替身可以用内存对象模拟 `localStorage`；故障注入可以主动制造写入失败，验证平时很难触发的回滚分支。
- 实际完成：新增 `package.json` 测试命令、`tests/runtime.cjs` 测试运行环境、金额工具测试和迁移测试；运行环境用 Node 的 `vm` 执行项目真实脚本，并用 `MemoryStorage` 提供与浏览器存储相同的基本接口。
- 验证方式：运行 `node --test --test-isolation=none tests/helpers.test.cjs tests/migrations.test.cjs`，6 项测试全部通过；同时对项目 JavaScript 和测试脚本执行语法检查。
- 遇到的问题：Node 测试运行器默认按文件启动子进程，当前受限环境返回 `spawn EPERM`；此外，项目使用按顺序加载的传统浏览器脚本，不能直接通过 CommonJS `require` 导入其中的全局函数。
- 解决方法：使用 `--test-isolation=none` 在当前进程运行测试；测试运行环境按页面依赖顺序把常量、状态、工具、存储适配层和迁移脚本加载进同一个 VM 上下文，因此测试直接调用真实代码，不复制业务实现。
- 面试表达：我为传统前端项目补充了零第三方依赖的自动化测试，用内存存储隔离用户数据，并通过故障注入验证迁移具有原子回滚能力；首批 6 个用例覆盖金额精度、两版升级、写入失败和损坏数据。
- 后续疑问：如何继续测试押金最终结算、账户余额和净资产等核心计算，并在 GitHub 上自动运行这些测试。

## 第 35 步：锁定押金、账户余额和净资产规则

- 日期：2026-09-13
- 目标：用自动化测试固定押金结算和账户余额规则，确保以后重构时不会把押金本金误算成收支，也不会把负债方向算反。
- 学到的知识：业务测试要围绕用户规则组织，而不只测试单个函数；资金账户和负债账户对同一笔资金流的方向相反；金额即使存成整数分，计算中途换回小数累加仍会产生精度误差。
- 实际完成：新增 `tests/records.test.cjs`，覆盖押金全额退回、分次退回后少退 300 元、收到押金后少退形成收入、资金与负债账户、还款转账和分值精度；测试运行环境开始加载真实的 `records.js`。
- 验证方式：运行完整测试集，12 项全部通过；其中新增 6 项核心计算测试，1000 元付出并全退的收支均为 0，累计退回 700 元只产生 300 元支出，资金账户 900 元减负债 300 元得到净资产 600 元，分次退回 0.10 元和 0.20 元的押金差额也能严格得到 0.30 元。
- 遇到的问题：精度用例首次失败，10 分和 20 分在余额函数中得到 `0.30000000000000004`，说明数据虽然保存为分，计算过程仍在累加元的小数。
- 解决方法：`computeBalancesFor()` 改为全程累加整数分，返回时才除以 100；`depositSettlementInfo()` 同样用分累计多次退回并计算最终差额，避免押金边界值出现小数尾差。
- 面试表达：我把押金业务规则写成回归测试，并通过 0.10 加 0.20 的失败用例发现计算层仍有浮点误差；随后将余额和押金差额改为整数分运算，完整 12 项测试通过。
- 后续疑问：如何让 GitHub 在每次提交和合并代码时自动运行这些测试，提前拦截回归。

## 第 36 步：加强备份结构校验和失败回滚测试

- 日期：2026-09-13
- 目标：在覆盖当前数据前确认备份的版本、存储键和内部对象结构有效，并自动验证写入失败不会破坏原数据。
- 学到的知识：语法正确的 JSON 不等于可用数据；边界校验需要同时检查容器类型、允许的键、字段类型和兼容版本；把数据替换抽成纯流程后，异常回滚更容易测试。
- 实际完成：为账本、账目、账户、自定义类型、自定义类别、当前账本和 schema 增加对应结构校验；拒绝未知记账本键和高于当前程序的备份版本；抽出 `replaceBackupStorage()` 负责快照替换与失败恢复；新增 `tests/backup.test.cjs`。
- 验证方式：完整测试集 17 项全部通过；备份测试覆盖当前格式、旧版扁平格式、缺少金额字段、未来版本、未知存储键和中途写入失败后的原快照恢复。
- 遇到的问题：原校验只确认键以 `jizhangben_` 开头、值是字符串并且可以执行 `JSON.parse`；记录数组被替换成错误结构、未知键或未来版本仍可能通过。
- 解决方法：根据每种正式存储键调用独立验证函数，并在任何写入前完成全部校验；替换失败时重新读取当前记账本键、清理已写部分，再恢复导入前快照。
- 面试表达：我把备份导入从 JSON 语法校验提升为领域结构校验，同时保留旧格式兼容，并通过故障注入证明导入写到一半时可以恢复原数据。
- 后续疑问：如何在 320px、375px 和 768px 三种宽度下完成手机端布局验收，正式收尾前端阶段。

## 第 37 步：完成手机和平板布局验收

- 日期：2026-09-13
- 目标：在 320px、375px 和 768px 三种视口下验证记账本没有横向滚动，主要控件便于触摸，并能完成真实记账流程。
- 学到的知识：响应式验收不能只看页面有没有溢出，还要测量按钮和输入框的实际尺寸；窄屏表单需要根据可用宽度改变排列方式；视觉检查和真实操作应该结合。
- 实际完成：在 `css/style.css` 增加三层响应式规则：768px 以下统一提供至少 44px 的触控区域，480px 以下缩小页面留白和金额字号，360px 以下把双列表单改成单列；统计金额和净资产区域允许在窄屏安全换行。
- 验证方式：Edge 中检查 3 种宽度 × 5 个页面，共 15 个布局场景；所有场景的页面宽度等于视口宽度、横向溢出元素为 0、低于 44px 的可见主要控件为 0。在 320px 下完成新增 1.23 元支出、查看统计、展开明细和删除记录，最终恢复空账本；控制台错误和警告均为 0。
- 遇到的问题：页面最初虽然没有横向滚动，但导航按钮约 42px 高，账户“改/删”按钮约 35×27px，备份按钮约 41px 高；768px 下也仍使用偏小的桌面控件尺寸。
- 解决方法：先用浏览器读取控件边界定位具体元素，再把触控规则覆盖到 768px；复测主页面后继续检查账本管理页，补齐备份按钮，直到三种宽度全部为 0 个小触控控件。
- 面试表达：我用真实浏览器建立 15 个响应式验收场景，同时检查横向溢出和 44px 触控区域；发现页面视觉正常但操作目标过小后，分层调整 CSS，并通过 320px 完整记账流程验证可用性。
- 后续疑问：如何开始阶段 3，设计 FastAPI 后端、SQLite 数据表和前后端接口，同时保留现有本地数据迁移能力。

## 第 38 步：设计后端、数据库和接口边界

- 日期：2026-09-13
- 目标：把 localStorage 中的数组和字符串关系转换成可维护的关系数据库模型，并确定 FastAPI 第一版接口范围。
- 学到的知识：关系数据库需要用主键、外键、唯一约束和事务表达数据边界；数据库约束适合保证单行结构，跨账本、转账和押金关联等规则仍要由服务层校验；接口模型不应继续同时传“元”和“分”。
- 实际完成：新增 `docs/BACKEND_DESIGN.md`，设计用户、账本、账户、类别、记账类型和账目六组数据表，明确押金自关联、账户删除、选项归档、整数分和 UUID 规则；列出健康检查、账本、账户、账目、选项、总览和备份 REST API；规划旧 ID 两遍映射迁移流程。
- 验证方式：逐项对照当前前端写入字段和路线图，确认金额、日期、转账账户、押金方向、原押金关联、自定义类型、类别和账本分类都有数据库去向；同时更新 README 中已经过时的单文件结构和押金说明。
- 遇到的问题：README 仍描述“全部代码在一个 index.html”，押金说明也还是旧的直接计入收支规则；如果据此设计 API，会把已经完成的模块化和最终差额结算重新做错。
- 解决方法：以当前生产脚本和已通过的业务测试为事实来源，先整理字段映射与关系，再同步 README；后端设计明确押金本金中性，只有最终差额进入收支。
- 面试表达：我没有直接把 localStorage 大对象搬进数据库，而是根据账本边界和业务关系设计规范化表结构，用自关联表示押金对账，用事务和两遍 ID 映射保证旧数据完整迁移。
- 后续疑问：如何建立最小 FastAPI 应用、SQLite 连接和健康检查，并用临时数据库自动测试而不接触真实账本。

## 第 39 步：建立最小 FastAPI 和 SQLite 运行骨架

- 日期：2026-09-13
- 目标：建立可独立启动和测试的后端，让健康检查真正连接 SQLite，同时不接触现有 localStorage 数据。
- 学到的知识：Python 虚拟环境可以隔离项目依赖；FastAPI 的 lifespan 适合执行启动初始化；应用工厂可以向测试注入临时数据库；健康检查只有执行真实查询才能证明数据库连接可用。
- 实际完成：新增 `backend/app/main.py`、`database.py`、项目专用虚拟环境说明、固定版本依赖和健康检查测试；SQLite 连接统一开启外键，初始化时建立 `schema_migrations` 表；`/api/health` 返回服务状态、数据库状态和 schema 版本。
- 验证方式：pytest 使用临时 SQLite 运行，结果为 `1 passed` 且无警告；随后实际启动 Uvicorn，请求 `/api/health` 得到 HTTP 200 和 `{"status":"ok","database":"ok","schema_version":0}`，并直接查询临时数据库确认迁移表存在。
- 遇到的问题：Python 的 `ensurepip` 和 pytest 在普通受限环境中无法写入临时目录；最初使用 `httpx` 的 FastAPI `TestClient` 还出现了当前 Starlette 的弃用警告。
- 解决方法：把安装和测试临时目录限制在项目内，并在需要时使用已授权环境执行；按照当前 Starlette 文档改用 `httpx2.AsyncClient` 和 ASGI transport，显式进入 FastAPI lifespan，测试通过且警告清零。
- 面试表达：我用应用工厂注入临时 SQLite，以异步 ASGI 客户端测试真实健康接口和启动生命周期；同时解决 Windows 临时目录权限与测试客户端弃用问题，确保新后端从第一步就可重复验证。
- 后续疑问：如何用版本化 SQL 迁移创建用户和账本表，并验证外键、唯一约束和重复执行安全性。

## 第 40 步：建立第一版用户和账本数据库迁移

- 日期：2026-09-13
- 目标：用可重复执行的版本化迁移创建用户和账本表，让数据库开始真实表达“用户拥有账本”的关系。
- 学到的知识：迁移版本表相当于数据库结构的履历；唯一约束负责阻止重复身份，外键负责阻止孤立数据，`ON DELETE CASCADE` 用于让父子数据按照明确规则一起删除；约束必须通过失败场景测试，不能只检查表存在。
- 实际完成：在 `backend/app/database.py` 增加 schema v1 迁移，建立 `users`、`books` 和 `books.user_id` 索引；邮箱使用不区分大小写的唯一约束，名称不能为空，账本外键指向用户并启用级联删除；应用启动时按版本运行尚未应用的迁移。
- 验证方式：后端 4 项测试全部通过，覆盖健康检查返回 schema 版本 1、重复初始化只保留一条版本记录、外键连接确实开启、大小写不同的同邮箱被拒绝、孤立账本被拒绝，以及删除用户后账本归零；原前端 17 项测试继续通过。
- 遇到的问题：受限环境仍无法重新访问 pytest 自己创建的临时目录，导致首轮显示 4 个 setup error；当前 PowerShell 会话也没有找到 `npm` 命令，但可以找到 Node 可执行文件。
- 解决方法：确认错误发生在测试临时目录准备阶段后，在已授权环境用明确的项目内 `basetemp` 运行；前端直接使用检测到的 Node 路径执行与 `package.json` 相同的测试参数；测试产生的两个根目录缓存经路径核对后清理。
- 面试表达：我实现了按版本执行的 SQLite 迁移，并用约束失败、级联删除和重复初始化测试证明用户与账本的数据边界有效；同时把测试环境错误与业务断言失败分开处理，最终后端 4 项、前端 17 项全部通过。
- 后续疑问：如何建立账户表，用检查约束限制资产和负债类型，并让账户随账本删除，同时为后续账目保留 `SET NULL` 关系。

## 第 41 步：建立账户表并约束金额与类型

- 日期：2026-09-13
- 目标：用 schema v2 建立账户表，让资金账户和负债账户归属于具体账本，并从数据库入口阻止无效类型和金额。
- 学到的知识：SQLite 的列类型使用类型亲和性，声明 `INTEGER` 本身仍可能接受小数；需要把 `typeof(...) = 'integer'` 与非负检查组合起来，才能确保金额始终保存为整数分。子表的级联删除规则应在建表时明确，而不是依赖调用者记得逐条清理。
- 实际完成：新增 schema v2 迁移，创建 `accounts` 表和 `book_id` 索引；账户名不能为空，`kind` 只能是 `asset` 或 `liability`，`initial_cents` 必须是非负整数，账户必须属于已有账本，删除账本时对应账户自动删除。
- 验证方式：后端测试从 4 项增加到 6 项，新增“已有 schema v1 升级到 v2”和账户领域约束测试；无账本、错误类型、负数本金、小数本金和空名称均被拒绝，两个合法账户可保存，删除账本后账户数为 0；原前端 17 项测试继续通过。
- 遇到的问题：如果只写 `initial_cents INTEGER CHECK (initial_cents >= 0)`，SQLite 仍可能保存 `1.5`，这会破坏项目已经统一的整数分规则。
- 解决方法：增加 `CHECK (typeof(initial_cents) = 'integer' AND initial_cents >= 0)`，并明确用 `1.5` 做失败测试；另用截断迁移列表建立真实 v1 临时库，再恢复完整迁移验证逐版本升级。
- 面试表达：我为账户模型增加数据库级领域约束，并针对 SQLite 动态类型补充整数检查；测试不仅覆盖全新建库，还从 schema v1 真实升级到 v2，证明迁移对已有数据库安全。
- 后续疑问：如何建立类别与记账类型表，既支持内置选项和用户自定义，又让归档选项继续保留历史统计含义。

## 第 42 步：建立类别和记账类型表

- 日期：2026-09-14
- 目标：用 schema v3 保存内置与自定义类别、记账类型，并通过归档保留历史账目的显示和统计含义。
- 学到的知识：软删除会把“名称是否唯一”变成带条件的规则；SQLite 部分唯一索引可以只约束 `archived_at IS NULL` 的有效数据，让旧记录继续存在，同时允许用户重新建立同名选项。显示名称可能变化，业务判断应依赖稳定代码和 `behavior`，不能依赖中文名称。
- 实际完成：新增 `categories` 与 `record_types` 表及用户索引；两表都记录所属用户、系统/自定义标记和归档时间；有效名称在同一用户下忽略大小写后唯一；记账类型另有不可重复的稳定 `code`，`behavior` 只允许收入、支出、冲减支出、转账、中性流出、中性和押金七种行为。
- 验证方式：后端测试从 6 项增加到 9 项，覆盖 schema v2 升级到 v3、孤立类别、空名称、错误系统标记、有效名称重复、归档后复用名称、类型代码重复、无效行为和用户删除级联；9 项后端测试及原有 17 项前端测试全部通过。
- 遇到的问题：如果直接给 `(user_id, name)` 加普通唯一约束，归档记录仍会占用名称，用户无法创建新的同名类别或类型；如果只保存中文类型名，改名和多语言显示还会影响业务判断。
- 解决方法：把名称唯一性实现为只覆盖未归档行的部分索引；另外用 `code` 作为稳定身份、`name` 负责显示、`behavior` 负责计算规则，并通过归档前后两次插入同名数据验证边界。
- 面试表达：我用部分唯一索引解决软删除与名称复用的冲突，并把记账类型拆成稳定代码、显示名称和业务行为，避免界面文案变化影响收支计算；同时验证已有 schema v2 数据库可安全升级到 v3。
- 后续疑问：如何建立账目表，表达转出/转入账户、类别、记账类型和押金自关联，并把数据库能保证的规则与服务层跨行校验清楚分开。

## 第 43 步：建立账目表并划分校验边界

- 日期：2026-09-14
- 目标：用 schema v4 建立核心账目表，保存金额、日期、类型、类别、账户流向和押金关联，同时明确数据库约束与服务层规则的边界。
- 学到的知识：数据库适合保证单行字段、外键和删除行为；“转账账户是否属于同一账本”“关联记录是否为方向匹配的原押金”等规则需要读取其他表或其他行，应由服务层在事务内判断。把所有规则硬塞进复杂触发器会降低可读性和测试能力。
- 实际完成：新增 `records` 表和六组查询索引；金额必须是大于 0 的整数分，日期必须是真实的 `YYYY-MM-DD`，两个账户不能相同，押金方向、对象、关联和最终结算标记必须形成合法组合；账本删除级联删除账目，账户删除把引用设为空，类别、类型和被关联的原押金在仍被使用时不能直接删除。
- 验证方式：后端测试从 9 项增加到 13 项，覆盖 schema v3 升级到 v4、缺失外键、零数/负数/小数金额、2 月 30 日、相同转账账户、五种错误押金组合、合法押金关联，以及账户、账本、类别、类型和原押金的删除行为；最终 13 项后端测试及 17 项前端测试全部通过。
- 遇到的问题：加入 schema v4 后，上一阶段的“v2 升到 v3”测试使用了全部最新迁移，因此实际升到了 v4，断言仍期待 `[1, 2, 3]`，首轮出现 1 项失败。
- 解决方法：让历史升级测试显式截取目标版本之前的迁移列表；v1→v2、v2→v3、v3→v4 分别固定终点，新迁移增加时不会悄悄改变旧测试的含义。
- 面试表达：我把账目模型中可由数据库保证的金额、日期、引用和押金字段组合固化为约束，把跨账本与跨记录业务判断留在服务层；同时修复了迁移测试随最新版本漂移的问题，让每条升级路径拥有固定验收终点。
- 后续疑问：如何建立服务层，在创建账目前确认类型行为、账户所属账本、类别所属用户和押金方向对应关系，并用事务返回清晰错误。

## 第 44 步：建立账目创建服务层

- 日期：2026-09-14
- 目标：在写入账目前完成数据库无法独立表达的跨表与跨记录校验，并向未来 API 提供稳定错误代码。
- 学到的知识：外键只证明某个 ID 存在，不能自动证明它属于当前用户或当前账本；服务层要从账本确定用户边界，再在该边界内查询类型、类别和账户。校验与写入必须使用同一个连接和事务，避免校验后数据状态变化或失败时留下部分结果。
- 实际完成：新增 `services/record_service.py`，用 `NewRecord` 表达待创建账目，用 `RecordValidationError` 返回稳定代码；服务会检查账本存在、类别/类型属于所有者且未归档、账户属于当前账本、类型是否要求账户、转账账户是否完整且不同，以及退回押金是否关联同账本、方向匹配的原押金；成功后生成或使用 UUID 并在同一事务写入。
- 验证方式：后端测试从 13 项增加到 28 项，覆盖合法支出、合法转账、缺失账本、跨用户或归档选项、跨账本账户、五类账户行为错误、合法押金退回、错误方向和不存在的原押金；所有校验失败均检查稳定错误代码，跨边界失败后账目数量仍为 0；原前端 17 项测试继续通过。
- 遇到的问题：原始外键会接受“确实存在但属于另一个用户”的类别或类型，也会接受属于另一个账本的账户；这些数据在数据库层看似完整，业务上却造成数据串户。
- 解决方法：先通过 `book_id` 查询唯一的 `user_id`，后续类别和类型查询都加入该用户条件，账户查询同时加入当前账本条件；对归档选项只允许历史账目继续引用，不允许新账目选择。
- 面试表达：我没有把外键存在等同于业务归属，而是在事务式服务层从账本推导用户边界，逐项校验类型、类别、账户和押金关联，并用稳定错误代码与失败不落库测试证明多用户隔离。
- 后续疑问：如何把账目服务接入 FastAPI，在请求结构校验、业务错误和数据库错误之间建立统一的 HTTP 状态码与响应格式。

## 第 45 步：建立第一个账目 REST API

- 日期：2026-09-14
- 目标：让客户端通过 HTTP 创建账目，并统一区分请求格式错误、资源不可用和业务冲突。
- 学到的知识：请求模型负责字段类型和格式，服务层负责跨表业务关系，数据库负责最终完整性；HTTP 状态码应表达失败类别，错误体还要提供稳定 `code`，手机端才能根据代码显示对应提示，而不是解析可能变化的中文文字。
- 实际完成：新增账目请求/响应模型和 records 路由；`POST /api/books/{book_id}/records` 将路径账本 ID 与 JSON 请求转换成 `NewRecord`，调用事务式服务并返回完整账目；全局异常处理把资源问题映射为 404、业务冲突映射为 409、Pydantic 字段错误映射为 422，错误体统一为 `code`、`message` 和可选 `details`。
- 验证方式：后端测试从 28 项增加到 35 项，覆盖 HTTP 201 和 UUID 落库、404 资源错误、409 缺少账户、0 金额、小数分和 2 月 30 日的 422，以及通过接口创建 1000 元原押金和关联 700 元最终退回；原前端 17 项测试继续通过。
- 遇到的问题：FastAPI 默认字段错误结构与业务错误结构不同；如果直接使用 `HTTPException(detail={...})`，业务错误还会额外嵌套在 `detail` 下，手机端需要维护多种解析方式。
- 解决方法：为 `RecordValidationError` 和 `RequestValidationError` 注册统一异常处理器，直接返回根级 `code` 与 `message`；字段错误额外保留 `details`，既方便用户提示，也保留调试位置。
- 面试表达：我完成了从 Pydantic 请求模型、业务服务、SQLite 事务到 HTTP 响应的第一条纵向链路，并用 201/404/409/422 场景验证统一错误契约；押金关联通过两次真实 API 请求完成，证明复杂业务规则没有停留在单元函数中。
- 后续疑问：如何为本地开发环境自动创建一个明确的开发用户，并提供账本查询和创建接口，让客户端不再依赖手工插入数据库 ID。

## 第 46 步：建立本地开发身份和账本 API

- 日期：2026-09-14
- 目标：让后端启动后自动具备明确的本地身份，并让客户端通过接口获得和创建属于该身份的真实账本。
- 学到的知识：身份解析应该集中在依赖层，路由只接收“当前用户”，服务层继续用该用户约束数据查询；这样阶段 4 接入登录会话时，只需替换身份来源。幂等初始化意味着同一启动操作执行多次，最终状态仍只有一份目标数据。
- 实际完成：应用 lifespan 会幂等建立固定本地开发用户，并把用户 ID 存入应用状态；新增 `GET /api/books` 和 `POST /api/books`，账本名称和分组会去除首尾空格；同名账本仍可创建，但返回 `duplicate_book_name` 警告；创建账目服务新增调用者用户参数，其他用户的账本统一按不存在处理。
- 验证方式：后端测试从 35 项增加到 42 项，覆盖重复启动只生成一个开发用户、空账本列表、创建与查询账本、同名警告、空名称 422、服务层和 HTTP 层跨用户账本拦截；前端 17 项测试继续回归。
- 遇到的问题：此前账目服务仅从传入的账本反查所有者，能保证关联对象一致，却没有证明发起请求的人就是该账本所有者；未来一旦接入多个登录用户，知道账本 ID 的人可能越权写入。
- 解决方法：用 FastAPI 依赖集中提供当前用户 ID，并显式传到服务层；查询账本时同时匹配 `book_id` 和 `user_id`，匹配失败返回与不存在相同的 404，既阻止写入也不泄露资源是否存在。
- 面试表达：我先建立可替换的身份依赖，再把用户边界传到业务服务；通过另一个真实用户和账本的负向测试证明，仅知道资源 ID 也不能越权写入，同时用幂等启动测试保证本地初始化不会制造重复用户。
- 后续疑问：如何建立账户查询和创建 API，并确保路径中的账本属于当前用户、账户本金始终使用非负整数分。

## 第 47 步：建立账本账户查询和创建 API

- 日期：2026-09-15
- 目标：让客户端能在自己的账本中读取和创建资金、负债账户，为后续通过 API 记账提供真实账户 ID。
- 学到的知识：嵌套路由中的 `book_id` 只是一个外部输入，必须在每次读取和写入前与当前用户一起校验。将通用业务异常独立出来，可以让账户、账目和未来模块复用同一种 HTTP 错误结构。
- 实际完成：新增账户服务、请求/响应模型和 `GET`、`POST /api/books/{book_id}/accounts`；账户名称去除首尾空格，类型只接受 `asset` 或 `liability`，初始金额只接受非负整数分且省略时为 0；业务异常处理从账目专用类型提升为可复用的公共类型。
- 验证方式：后端测试从 42 项增加到 49 项，覆盖空或不存在账本、资金和负债账户创建、默认本金、查询排序、跨用户账本访问、空名称、错误类型、负数和小数分；前端 17 项回归测试全部通过。
- 遇到的问题：账户表已有外键和金额约束，但如果接口直接按 `book_id` 写入，知道他人账本 ID 的请求仍可能在权限层越界；而把错误处理继续命名为“账目错误”也会让新模块语义混乱。
- 解决方法：账户服务先用 `(book_id, current_user_id)` 查询所有权，再读写账户；无论账本不存在还是不属于当前用户，都返回 `book_not_found`。同时新增 `BusinessValidationError` 作为所有业务模块的公共错误基类，保留原账目错误类型兼容已有测试。
- 面试表达：我为嵌套账户 API 建立了从当前身份到父账本再到账户的授权链，并通过跨用户负向测试证明不能仅凭资源 ID 越权；同时抽取公共业务异常，使 API 错误契约可以随着模块增加而保持一致。
- 后续疑问：如何提供类别和记账类型查询接口，只返回当前用户未归档的可选项，并让前端记账表单逐步改用服务端数据。

## 第 48 步：建立类别和记账类型查询 API

- 日期：2026-09-15
- 目标：让客户端能从后端读取当前用户可用于新建账目的类别和记账类型，不再假设所有选项都来自浏览器本地数据。
- 学到的知识：类别和记账类型属于用户级资源，因此查询条件应直接从当前用户开始；归档是保留历史的机制，列表接口必须默认排除归档项，避免它们重新出现在新账目表单。显示名称用于界面，类型的稳定 `code` 与 `behavior` 才用于业务判断。
- 实际完成：新增选项服务、响应模型及 `GET /api/categories`、`GET /api/record-types`；两条接口均只返回当前用户的未归档记录，系统项排在自定义项之前。类型响应返回 `code`、`name`、`behavior` 和系统标记。
- 验证方式：后端测试从 49 项增加到 52 项，覆盖空列表、当前用户系统/自定义项排序、归档项过滤、其他用户数据隔离，以及类型行为字段；前端 17 项回归测试全部通过。
- 遇到的问题：账户列表原先按 `created_at, id` 排序，但 SQLite 默认时间精度只有秒；同一秒新建两个账户时 UUID 会参与排序，导致列表顺序不稳定，第一次完整回归恰好发现此问题。
- 解决方法：账户列表改为按 SQLite 的实际插入顺序 `rowid` 返回；修复后重新运行完整后端测试，52 项全部通过。对于类别和类型，排序使用确定的“系统标记、名称、ID”规则。
- 面试表达：我将用户级选项与账本级资源分开设计查询边界，默认过滤归档数据并返回类型的稳定业务行为；同时通过回归失败发现时间戳精度不足造成的不稳定排序，改用明确排序规则恢复可预测的客户端列表。
- 后续疑问：如何提供类别和记账类型的创建 API，校验名称、稳定代码和行为，同时保留系统项与自定义项的边界。

## 第 49 步：建立自定义类别和记账类型创建 API

- 日期：2026-09-15
- 目标：让用户能通过后端新增自己的类别和记账类型，同时保持系统内置规则、稳定代码和收支行为不被误改。
- 学到的知识：请求模型既要校验字段格式，也要限定业务允许的选择范围；数据库唯一约束是最后防线，服务层应提前识别名称或代码冲突，并返回客户端可处理的稳定错误代码。系统数据与用户自定义数据必须由服务端决定，不能相信请求中的标记字段。
- 实际完成：新增 `POST /api/categories` 和 `POST /api/record-types`；类别请求只接收名称，类型请求接收小写稳定代码、显示名称和收入/支出/中性行为。服务端固定写入 `is_system = 0`，并返回新对象的 UUID、所属用户和创建时间。
- 验证方式：后端测试从 52 项增加到 59 项，覆盖创建后查询、类别忽略大小写重名、类型代码与名称冲突、空名称、非法大写代码和不允许的押金行为；前端 17 项回归测试全部通过。
- 遇到的问题：数据库确实能拒绝重复数据，但直接把数据库完整性错误暴露给 HTTP 客户端会包含底层实现细节，前端也无法稳定判断是名称还是代码冲突。
- 解决方法：服务层先按当前用户检查类别有效名称、类型代码和类型有效名称，分别抛出 `category_name_conflict`、`record_type_code_conflict`、`record_type_name_conflict`；公共异常处理器将它们统一转换成 409。
- 面试表达：我把自定义选项创建分成请求格式、业务冲突和数据库写入三层处理，既利用数据库保护唯一性，又为客户端提供稳定的冲突代码；同时由服务端强制自定义标记和允许行为，避免用户请求越过系统规则。
- 后续疑问：如何提供账目查询 API，支持日期、账户、类别和关键字筛选，并在返回时保持当前用户的账本边界。

## 第 50 步：建立账目查询和筛选 API

- 日期：2026-09-15
- 目标：让客户端按自己的账本读取已保存账目，并复现前端已有的日期、账户、类别和关键词筛选能力。
- 学到的知识：读接口同样需要先建立用户与账本边界，不能因为“只是查询”就跳过授权；列表接口应返回页面实际需要的显示信息，避免客户端为每条账目重复请求类型、类别和账户。动态筛选条件只能拼接预先写好的 SQL 片段，用户输入必须始终作为参数传入。
- 实际完成：新增 `GET /api/books/{book_id}/records`，支持 `from`、`to`、`account_id`、`category_id`、`q` 参数；关键词搜索备注、类别名称和类型名称。接口关联类型、类别及转出/转入账户，返回稳定代码、行为和显示名称；结果按日期和实际创建顺序倒序排列。
- 验证方式：后端测试从 59 项增加到 62 项，先通过创建 API 建立三条不同日期、账户、类别和类型的账目，再覆盖完整列表、五种筛选、跨用户账本 404、日期范围倒置和非法日期 422；前端 17 项回归测试全部通过。
- 遇到的问题：创建账目的响应只包含外键 ID，若列表接口照搬该结构，手机端渲染一条明细至少还要查类型、类别和账户，列表越长请求越多且容易显示不同步。
- 解决方法：列表 SQL 使用关联查询一次取回类型代码/名称/行为、类别名称和两个账户名称；账户使用左连接，以兼容历史账目因账户删除而留下的空引用。
- 面试表达：我把账目查询设计为以账本为授权边界、以参数化条件组合筛选、以关联查询补齐展示字段的单次接口；并用真实创建后的多场景数据测试日期、账户、类别和关键词筛选。
- 后续疑问：如何实现账目的修改和删除，同时重新执行同一套账户、类型、类别和押金关联校验，避免编辑绕过创建规则。

## 第 51 步：建立安全删除账目 API

- 日期：2026-09-15
- 目标：让用户能够纠正误记账目，同时不允许删除已被押金结算记录引用的原押金。
- 学到的知识：删除不是简单执行一条 SQL；需要先验证当前用户与账本边界、目标记录存在性，以及是否有其他业务记录依赖它。数据库外键能阻止引用错误，但服务层应提前返回用户能理解的业务原因。
- 实际完成：新增 `DELETE /api/books/{book_id}/records/{record_id}`。普通账目删除成功返回空响应 204；账目不存在返回 `record_not_found`；若任意记录以它为 `deposit_link_id`，返回 `deposit_record_in_use` 并拒绝删除。
- 验证方式：后端测试从 62 项增加到 64 项，覆盖普通账目删除、重复删除 404、原押金被关联退回记录阻止删除、先删退回记录后可删原押金；前端 17 项回归测试全部通过。真实 HTTP 服务也确认删除返回 204、响应正文为空、列表剩余 0 条。
- 遇到的问题：仅依靠 SQLite 外键来阻止删除时，客户端会得到底层完整性错误，无法知道该先处理哪条业务关系。
- 解决方法：删除前显式查询 `deposit_link_id` 引用，使用稳定的 `deposit_record_in_use` 和中文说明返回 409；这样界面可提示用户先删除或调整关联退回记录。
- 面试表达：我把删除接口设计为“授权检查、存在性检查、业务依赖检查、执行删除”四步，并用押金结算链路证明不会因删除父记录而破坏对账数据。
- 后续疑问：如何实现账目修改，复用创建校验并锁定已参与押金结算的记录，避免编辑后让已有关联失效。

## 第 52 步：建立安全修改账目 API

- 日期：2026-09-15
- 目标：让用户可以更正普通账目的金额、日期、账户、类别、类型和备注，同时不破坏已经完成的押金结算关系。
- 学到的知识：更新接口不能绕过创建接口的领域规则；最可靠的做法是把路径 ID 固定为原记录身份，构造新的待校验对象并复用同一套校验。涉及多条记录的业务关系需要定义锁定边界，而不是只验证单行字段。
- 实际完成：新增 `PUT /api/books/{book_id}/records/{record_id}`，请求体复用创建账目结构，更新时重新验证账本归属、类别、类型、账户和押金规则，并设置新的更新时间。已被退回记录引用的原押金返回 `deposit_record_in_use`，已关联原押金的退回记录返回 `deposit_settlement_locked`。
- 验证方式：后端测试从 64 项增加到 67 项，覆盖正常修改、无效修改不改变原数据、原押金和退回记录两端锁定；前端 17 项回归测试全部通过。真实 HTTP 验证中，同一 UUID 账目成功从 1200 分“日常支出”更新为 5600 分“兼职收入”。
- 遇到的问题：如果先读取记录再只更新用户传入的几个字段，容易遗漏字段组合校验，也会让旧押金字段和新类型不匹配；更严重的是，修改押金结算链路的一端可能让另一端的方向关系失效。
- 解决方法：采用完整替换而非部分拼接，始终调用 `validate_new_record()`；对结算链路两端在更新前明确检查并锁定，避免出现一半有效的历史关系。
- 面试表达：我将账目更新设计成对创建规则的复用，并为押金这种跨记录业务建立双向修改锁；测试证明无效更新不会污染原数据，复杂结算记录也不能被任意编辑。
- 后续疑问：如何完善账本和账户的修改、删除 API，并针对“账户仍被历史账目引用”的情况采用清晰的保留或清空策略。

## 第 53 步：完成账户修改、删除与历史引用保护

- 日期：2026-09-15
- 目标：让用户能修改或删除账本账户，同时保留历史账目，不因账户管理操作丢失记账记录。
- 实际完成：新增账户 `PATCH` 和 `DELETE` 接口；修改名称、类型和本金时保留账户 ID，删除后返回 204。SQLite 外键规则会将历史账目的账户引用自动设为空。
- 验证方式：后端测试从 67 项增加到 69 项，覆盖修改、删除、重复删除 404，以及删除被历史账目引用的账户后账目仍存在且 `account_id` 为 null；前端 17 项回归保持通过。
- 学到的知识：删除子资源时需要区分“删除账户对象”和“删除历史业务记录”。通过 `ON DELETE SET NULL` 保留账目，再由列表接口的左连接展示未指定账户，能兼顾数据完整性与历史可读性。
- 面试表达：我为账户 CRUD 增加了历史账目保护，删除账户不会删掉记账记录，而是清空已失效引用；测试直接检查删除后的数据库状态，证明规则真的执行。
- 后续疑问：如何建立账本的修改和删除 API，并验证删除账本会按设计级联清理其账户和账目。

## 第 54 步：完成账本修改、删除与级联清理

- 日期：2026-09-15
- 目标：让用户能改名、调整分组或删除自己的账本，并让删除行为与账本作为账户和账目的父资源这一关系保持一致。
- 实际完成：新增 `PATCH /api/books/{book_id}` 和 `DELETE /api/books/{book_id}`。修改时保留账本 ID，使用与创建相同的去空格字段规则；若与另一份账本忽略大小写后同名，仍完成修改并返回 `duplicate_book_name` 提示。删除前按当前用户和账本 ID 一起验证所有权，成功返回 204，由 SQLite 外键级联删除其账户和账目。
- 验证方式：后端测试从 69 项增加到 71 项，覆盖同名修改提示，以及删除账本后账本、账户和账目均被清理；前端 17 项回归测试全部通过。另以临时数据库启动真实服务，确认 PATCH 返回同名提示、DELETE 返回 204，列表只保留未删除账本。
- 学到的知识：删除父资源与删除子资源的策略不同。账户删除采用 `SET NULL` 来保留账目历史；账本本身则是一个完整账目空间的根，删除它应以数据库外键的 `CASCADE` 清理其下属数据。服务层仍需先做所有权检查，不能把级联删除当作授权判断。
- 遇到的问题：PowerShell 的 `New-Item` 不支持 `-LiteralPath` 参数，临时目录已经存在时会打印参数提示；服务仍使用已存在的临时目录和独立数据库完成了接口验证。
- 解决方法：后续创建临时目录时使用 `-Path`，并在步骤结束后显式清理固定的项目内临时路径，避免测试文件混入真实数据目录。
- 面试表达：我根据资源层级区分删除语义：账户删除保留历史账目，账本删除作为聚合根级联清理其子数据；两个 API 都先验证当前用户对父资源的所有权，并通过真实 HTTP 和数据库断言验证 204、同名提示与级联结果。
- 后续疑问：如何让用户修改或归档类别和记账类型，同时保留历史账目引用和稳定的业务行为。

## 第 55 步：完成类别和记账类型修改、归档

- 日期：2026-09-15
- 目标：让用户能修正自定义选项，同时停用不再使用的选项，不破坏已经保存的历史账目。
- 实际完成：新增类别和记账类型的 `PATCH`、`DELETE` 接口。类别可修改名称；记账类型只修改显示名称，稳定 `code` 和 `behavior` 保持不变。`DELETE` 实际写入 `archived_at`，不物理删除；系统内置项、其他用户资源和已归档项会被拒绝。
- 验证方式：后端测试从 71 项增加到 77 项，覆盖名称修改、同名冲突、系统项保护、跨用户 404、归档后有效列表过滤和历史账目保留；前端 17 项回归测试继续通过。真实 HTTP 服务确认类别、类型修改返回 200，归档返回 204，归档后两个有效选项列表均为空。
- 学到的知识：选项删除需要区分“从新建表单隐藏”和“从数据库抹掉”。历史账目依赖类别和类型的外键，所以用归档保留原对象；记账类型的显示名称可以变，稳定代码和收支行为必须保持，避免客户端判断逻辑或历史统计含义漂移。
- 遇到的问题：如果直接复用创建请求作为记账类型更新请求，客户端可以提交新的 `behavior`，从而把收入类型改成中性或支出，历史数据的统计解释也会发生变化。
- 解决方法：为更新单独定义请求模型，只允许修改名称；服务层再检查当前用户、系统标记、归档状态和有效名称冲突。归档操作只更新时间字段，查询接口继续使用 `archived_at IS NULL` 过滤。
- 面试表达：我把可编辑展示属性与不可变业务属性分开建模，用归档而不是物理删除保护历史外键引用，并通过 200/204、跨用户 404、系统项 409 和数据库历史记录断言验证完整行为。
- 后续疑问：如何把账户本金、账目行为、转账和押金结算统一成后端余额与总览接口，并与前端现有结果逐笔对账。

## 第 56 步：建立后端余额和总览 API

- 日期：2026-09-15
- 目标：让服务端统一返回各账本的账户余额、期间收入、期间支出和净资产，并与现有网页的计算口径一致。
- 实际完成：新增 `GET /api/overview` 和总览服务。响应按账本返回账户的 `balance_cents`、`income_cents`、`expense_cents`、`net_worth_cents`，并提供全部账本合计；默认统计本月，也支持 `from`、`to` 日期范围。
- 验证方式：后端测试从 77 项增加到 80 项，覆盖资产和负债账户符号、收入、支出、退款、转账、上月账目、押金 10000 分最终只退 7000 分计 3000 分支出、跨用户账本过滤和日期范围错误；前端 17 项回归测试继续通过。真实 HTTP 请求创建 10000 分现金账户并记 1000 分支出，确认总览返回余额和净资产各 9000 分。
- 学到的知识：账户余额和收支统计是两个维度。所有历史账目都要参与余额，只有所选期间的行为影响期间收支；负债账户要反转流入和流出对欠款的影响，转账只改变两个账户余额，不能增加收入或支出。
- 遇到的问题：如果把押金原始流出直接当成支出，或者把每次退回都当成收入，会与前端“押金本金只算资金流、最终差额才计收支”的规则冲突。
- 解决方法：总览服务先按类型行为计算账户流向，再单独处理标记最终结算的退回押金，使用原押金金额减去所有关联退回金额的非负差额，并按原押金方向计入收入或支出。对旧数据中的不完整转账，只有两个账户都存在时才应用双边余额变化，避免出现单边扣款。
- 面试表达：我把余额和期间统计拆开计算，在服务端用整数分重放账目流向，明确处理负债、转账和押金差额；测试既检查每个账户，也检查账本和全部账本的汇总，并用真实 HTTP 请求验证接口可运行。
- 后续疑问：如何固定服务端备份导出、导入校验和旧 ID 映射契约，让网页可以安全从 localStorage 迁移到后端。

## 第 57 步：建立服务端备份导出、导入和 ID 映射

- 日期：2026-09-15
- 目标：固定后端备份 JSON 结构，让网页以后可以安全迁移数据，并在导入失败时保护当前账本。
- 实际完成：新增 `GET /api/backups/export` 和 `POST /api/backups/import`。备份格式为 `jizhangben-server-backup` 版本 1，只导出当前用户的账本、账户、类别、记账类型和账目；导入时生成新的 UUID，通过 `id_map` 重建账户、转账和押金关联，并替换当前开发用户的服务端数据。
- 验证方式：后端测试从 80 项增加到 84 项，覆盖当前用户隔离、完整导出、旧账本替换、押金关联按依赖顺序恢复、非法关系拒绝和不支持版本拒绝；前端 17 项回归测试继续通过。真实 HTTP 闭环先创建账本和账目，再导出、增加干扰账本、导入备份，确认只剩原备份账本、账目数量恢复且净资产保持 4200 分；另确认旧 ID 被映射为新的 UUID。
- 学到的知识：备份不仅是把表逐个序列化，还要定义版本、数据边界和恢复策略。导入关系数据时要先导入父表，再按 `deposit_link_id` 的依赖顺序导入账目；不能相信备份中的用户 ID，必须由当前登录身份重新赋值。
- 遇到的问题：真实 HTTP 验证第一次读取 PowerShell 对象属性的方式不正确，误把返回的 ID 映射显示为空；重新按对象属性名读取后确认服务端确实返回旧 ID 到新 UUID 的映射。
- 解决方法：将映射作为结构化响应的一部分，并用数据库查询和再次导出验证恢复结果，而不是只依赖终端打印的一行文字。所有校验在删除当前数据前执行，校验失败返回统一 422。
- 面试表达：我设计了带版本号的服务端备份契约，按当前用户隔离导出，导入前验证跨表和押金关系，事务内生成新 ID 并重建关联；测试和真实 HTTP 闭环同时证明了替换、回滚保护和数据一致性。
- 后续疑问：如何在网页中建立后端数据访问层，先切换账本和账户读取，再用不含真实隐私的备份演练 localStorage 迁移。

## 第 58 步：建立网页后端读取层（账本和账户）

- 日期：2026-09-15
- 目标：让网页在具备 HTTP API 时先读取服务端账本和账户，同时保留直接打开 `index.html` 和网络失败时的 localStorage 回退。
- 实际完成：新增 `js/api-client.js`，集中处理 API 基地址、JSON 请求、服务端字段映射和异步启动加载；启动流程先同步渲染本地数据，再尝试 `GET /api/books` 与 `GET /api/books/{book_id}/accounts`。成功读取后刷新账本下拉、账户下拉和账户列表，并显示“后端读、本地写”的阶段提示。
- ID 兼容：后端账本和账户主键是 UUID 字符串，网页不再对账本、账户下拉值使用 `Number(...)`，查找和账户余额计算按字符串化 ID 比较；原有 localStorage 数字 ID 仍可继续使用。
- 回退规则：`file://` 页面没有同源 API，客户端会直接停用请求；HTTP/HTTPS 请求失败、响应结构异常或账本列表为空时，不替换当前本地数据。当前新增、修改和账目明细继续走本地存储，避免半迁移写入服务端。
- 验证方式：新增 5 个前端 API 客户端测试，覆盖文件模式停用、账本和账户映射、非空账本替换、空响应保留、UUID 账户读取和账户请求失败时的整体回退；前端测试从 17 项增加到 22 项并全部通过，后端 84 项回归和 Python 编译检查也通过。
- 遇到的问题：第一次测试用 Node 的 `deepStrictEqual` 比较 VM 沙箱对象时，即使字段完全相同也被判为不同引用，这是跨 JavaScript realm 的原型差异，不是映射错误。
- 解决方法：测试跨沙箱结果时改用 `JSON.stringify` 比较结构；同时补齐 UUID 事件处理，避免出现“服务端列表能显示但无法切换账户”的半成功状态。
- 面试表达：我用渐进式数据访问层降低迁移风险，让本地页面先可用、后端读取异步验证、失败自动回退；在接口 ID 契约不一致时把 ID 当作不透明字符串，并用映射和回退测试固定行为。
- 后续疑问：如何接入服务端类别、记账类型和账目读取，并设计一份不含真实隐私的数据副本完成数量、收支、押金结算和净资产对账。

## 第 59 步：扩展网页读取层到选项、明细和总览

- 日期：2026-09-15
- 目标：让网页使用同一套后端访问层读取类别、记账类型、当前账本明细和跨账本总览，同时继续支持本地数据回退。
- 实际完成：新增服务端类别、记账类型、账目和总览响应映射；启动时依次读取账本、选项、账户、明细和总览，全部成功后才替换内存状态。自定义选项写入现有 `dataState`，系统选项继续使用前端内置列表；总览面板在服务端读取成功时使用 `/api/overview` 的账本和合计数据。
- 业务映射：后端 `asset/liability` 映射为网页的“资金/负债”，整数分转成同时包含 `amountCents` 和元金额的记录；`receive/return_to_other/pay/returned_to_me` 映射为“收/退/付/退回”，保留押金关联和最终结算标记。
- 整体切换：启动前保存本地账本、账户、选项、明细和总览快照；任一接口响应结构异常或请求失败，都恢复整组本地状态，不显示只切换了一部分的页面。切换服务端账本时，账户和明细请求也采用成组恢复。
- 验证方式：新增选项、明细和总览映射测试，以及有效自定义选项过滤、押金方向转换、UUID 账户引用和异常明细回退测试；前端测试从 22 项增加到 28 项并全部通过，所有 JavaScript 文件语法检查通过，后端 84 项回归保持通过。
- 遇到的问题：只读取服务端当前账本明细会让原有“跨账本总览”继续从 localStorage 计算，导致统计卡片和总览口径不一致；同时服务端选项包含系统项，直接追加会与前端内置选项重复。
- 解决方法：把总览接口一起纳入读取批次，在总览模块使用服务端摘要；选项映射时只把非系统项放入自定义数组，系统项仍由常量提供。这样当前页面的明细、统计卡片和总览都来自同一服务端快照。
- 面试表达：我把 API 适配从单个资源读取扩展为一组可验证的数据快照，用字段映射隔离后端命名差异，并以整体提交和整体回退避免前后端迁移期间出现跨模块不一致。
- 后续疑问：如何用脱敏副本演练 localStorage 到服务端的 ID 映射和收支对账，再安全切换新增账本、账户和账目的写入路径。

## 第 60 步：用脱敏副本演练 localStorage 到服务端迁移

- 日期：2026-09-15
- 目标：在不读取真实浏览器数据和正式数据库的前提下，走通旧数字 ID 到服务端 UUID 的转换、跨表关联恢复、统计对账和失败回滚。
- 实际完成：新增 `backend/app/services/local_backup_migration.py`，提供 `convert_local_backup()`。它读取网页导出的 `jizhangben-backup` 或旧版扁平 localStorage 对象，生成服务端备份 v1；本地账本、账户和账目 ID 会带上账本作用域，内置类型、类别、默认账户、自定义选项和押金方向都会转换成后端字段。
- 对账样本：用虚构的“脱敏日常账”建立 2 个账户、4 笔账（1200 分支出、10000 分押金支付、7000 分押金退回并最终结算、5000 分转账）。导入临时 SQLite 后，账目数为 4，期间支出为 31200 分，净资产为 168800 分；押金少退的 3000 分只计支出，转账不增加支出。
- ID 映射：来源 ID 使用 `book:10`、`account:10:1`、`record:10:11` 等带账本作用域的字符串，服务端导入再生成 UUID，并用响应中的 `id_map` 重建转账两端和押金 `deposit_link_id`。这样不同账本可以拥有同名数字账户而不会冲突。
- 回滚验证：故意把账户的 `book_id` 改成不存在的值，服务端返回 `backup_invalid` 和 422；原有服务端账本仍然存在，没有被导入前的清理操作影响。
- 验证方式：新增 5 个迁移测试，覆盖完整转换、旧版扁平备份、未知类型拒绝、真实临时 API 导入对账和无效引用回滚；后端测试从 84 项增加到 89 项并全部通过，Python 编译检查通过。
- 遇到的问题：本地账户 ID 在不同账本中可能重复，直接把数字 ID 当作服务端全局 ID 会让关系串账；另外网页默认账户没有写入 localStorage，导出文件里可能没有账户数组。
- 解决方法：为账户和账目来源 ID 加上所属账本前缀，并在转换器中把缺失或空账户列表还原为网页默认账户；所有关系先在转换阶段解析，再交给服务端事务导入。
- 面试表达：我把迁移拆成“脱敏副本转换、临时库导入、数量和金额对账、故意失败回滚”四步，没有直接在真实数据上试错；通过作用域化来源 ID 和服务端 `id_map`，解决了旧数字主键与后端 UUID 以及跨表关系的兼容问题。
- 后续疑问：如何把这个离线转换器接到网页的迁移向导，并在真正切换写入前增加重复提交保护、迁移确认和可回退开关。

## 第 61 步：增加只读迁移预览接口

- 日期：2026-09-15
- 目标：让网页在真正导入前得到可读的数量和金额对账结果，同时保证预览不会覆盖当前服务端数据。
- 实际完成：新增 `POST /api/backups/preview` 和 `POST /api/backups/preview-local`。前者校验服务端备份，后者直接接收网页的 `jizhangben-backup` 并调用本地备份转换器；两者都返回总数量、分账本账户和账目数量、收入、支出与净资产摘要。
- 业务规则：预览使用与服务端总览相同的整数分计算口径，转账不计收支，押金只在最终结算时计算少退或多收的差额；预览阶段不生成目标 UUID，也不执行删除或写入。
- 验证方式：新增迁移预览 API 测试，脱敏样本返回 1 个账本、2 个账户、4 笔账目，支出 31200 分、净资产 168800 分；随后查询服务端账本仍为空，证明接口没有写入。后端完整测试从 89 项增加到 90 项并全部通过。
- 遇到的问题：如果预览接口复用导入接口，为了计算摘要而先清理当前数据，会让用户在还没确认时失去回退点；如果另写一套金额规则，又可能与正式总览出现差异。
- 解决方法：把预览限制为“校验 + 内存计算”，显式返回对账摘要；转换器和服务端校验仍沿用同一份备份契约，押金结算用专项测试固定口径。正式导入仍只由事务式 `/api/backups/import` 完成。
- 面试表达：我为数据迁移增加了只读预览阶段，先验证格式和跨表关系，再用整数分计算可审计的数量、收支和净资产摘要；只有用户确认后才进入事务导入，因此预览失败或用户取消都不会污染正式数据。
- 后续疑问：如何在网页中把预览结果做成可理解的确认卡片，并防止用户重复点击导入造成重复迁移。

## 第 62 步：把迁移预览接入网页确认流程

- 日期：2026-09-15
- 目标：让用户可以在账本管理页面选择备份、查看服务端对账摘要，再明确确认是否导入。
- 实际完成：备份区域新增“预览迁移”按钮和迁移文件选择器；页面先本地检查备份结构，再调用 `/api/backups/preview-local`。预览卡片显示账本、账户、账目数量，以及总收入、总支出、净资产和每个账本的摘要。
- 确认边界：预览成功后才启用“确认导入到服务端”；确认按钮调用 `/api/backups/import-local`，导入期间禁用按钮防止重复提交。取消预览、文件错误、后端不可用或导入失败都不会清除浏览器 localStorage。
- 读取方式：导入成功后重新执行整组服务端读取，把账本、账户、选项、明细和总览一起刷新；如果刷新失败，页面提示重新打开，保留用户原始备份作为回退点。
- 验证方式：新增前端 POST 请求、错误代码和金额格式测试，前端测试从 28 项增加到 30 项；后端新增本地备份确认导入测试，完整后端测试从 90 项增加到 91 项；所有 JavaScript 文件语法检查通过。
- 遇到的问题：旧的“导入备份”按钮是直接恢复浏览器 localStorage，不能混用为服务端迁移；另外直接打开 `index.html` 时没有 HTTP API，预览按钮必须给出可理解的提示。
- 解决方法：保留原有本地备份导入按钮，新增独立的迁移预览流程；客户端根据页面协议停用后端请求，并把服务端返回的 `backup_invalid` 显示为用户可读错误。迁移确认只提交原始备份一次，服务端负责转换和事务导入。
- 面试表达：我把迁移设计成页面上的显式状态机：选择文件、结构预检、服务端只读预览、用户确认、禁用重复提交、事务导入和整组重新读取；每个失败节点都有本地备份和服务端数据保护。
- 后续疑问：如何先把新增账本和账户改为后端写入，同时保留网络失败时的本地回退和成功后的状态同步。

## 第 63 步：新建账本和账户优先写入后端

- 日期：2026-09-15
- 目标：在不一次性切换全部写入逻辑的前提下，让新建账本和新建账户先进入服务端，并保留网络失败时的本地回退。
- 实际完成：扩展 `js/api-client.js` 的 JSON 写入方法，增加账本和账户的创建、修改路径；当前表单只把“新建账本”和“新建账户”优先发送到后端，成功后用响应中的 UUID 更新内存页面。已有对象的修改、删除和账目写入仍保持原流程。
- 回退规则：服务端请求失败时，页面恢复写入前的 localStorage 快照，关闭后端读取标记，再继续执行原来的本地保存；提示中明确说明本次只保存在浏览器本地，避免用户误以为服务端也已更新。
- 一致性处理：新建成功后重新读取服务端总览，确保新账本或新账户的净资产变化不会让总览卡片停留在旧摘要；账户金额继续使用整数分提交。
- 验证方式：新增 API 写入路径、UUID 编码、服务端成功和网络失败回退测试，前端测试从 30 项增加到 33 项并全部通过；所有 JavaScript 文件语法检查通过，后端 91 项回归保持通过。
- 遇到的问题：服务端读取成功后，页面里的账本 ID 可能已经是 UUID；如果失败回退时直接在这份内存数组上生成本地数据，刷新后会把服务端快照和本地快照混在一起。
- 解决方法：失败时先从 localStorage 重新加载整组本地状态，再执行原有保存函数；成功时不覆盖本地原始备份，只在当前内存中使用服务端响应，并刷新服务端总览。
- 面试表达：我采用渐进式写入切换，先把低风险的新建账本和账户接入后端，利用响应 UUID 更新状态；失败时回到完整本地快照，避免 UUID 与旧数字 ID 混用，也避免出现一半写入成功的页面。
- 后续疑问：如何把包含押金和转账校验的账目保存接到后端，并在提交成功后处理服务端返回的完整记录和重复点击。

## 第 64 步：新建账目优先写入后端

- 日期：2026-09-15
- 目标：让普通支出、收入、转账和押金新建记录进入服务端，同时不破坏现有整数分计算和押金对账规则。
- 实际完成：`js/api-client.js` 增加账目创建请求，并在读取服务端类别和记账类型时保存“名称 → UUID”映射。`js/record-form.js` 在新建记录时把类型、类别、账户、转账目标、整数分金额、日期和押金字段转换为 `RecordCreateRequest`；服务端成功后使用返回的记录 UUID 和表单名称更新页面。
- 业务映射：普通支出/收入、自定义收支类型、转账和四种押金方向分别使用后端稳定 `behavior` 与 `deposit_direction`；退回押金继续提交 `deposit_link_id` 和 `deposit_final`，因此本金与最终差额的统计口径不在前端写入时改变。
- 回退规则：若服务端选项没有对应 UUID，或创建请求失败，页面先从 localStorage 恢复账本、账户和记录，再把服务端账户 UUID 按账户名称转换回本地 ID，然后执行原来的本地保存并提示“已保存到本地”。
- 验证方式：前端测试从 33 项增加到 36 项，覆盖普通记录请求、押金方向和关联 ID、网络失败后的本地账户引用恢复；`npm test` 通过 36 项，所有 JavaScript 文件语法检查通过，后端 91 项回归保持通过。
- 遇到的问题：服务端的创建响应只返回类型 ID 和类别 ID，不包含网页显示所需的名称；同时网络失败时当前内存账户可能是 UUID，不能直接把它写回使用数字 ID 的 localStorage。
- 解决方法：客户端用本次表单选择的名称补全响应映射；回退前记录账户名称和原押金的业务指纹，恢复本地快照后重新解析账户与押金关联，避免 UUID 和旧数字 ID 混用。
- 面试表达：我把前端表单模型转换为后端领域请求，保留类型/类别 UUID 和押金关联字段；成功响应采用服务端主键，异常时先恢复本地快照再回退，测试同时覆盖普通记录、押金和主键类型转换。
- 后续疑问：如何在账目保存按钮上增加重复提交保护，并在保存成功后重新读取服务端明细，处理修改和删除仍在本地造成的状态差异。

## 第 65 步：防止重复提交并重新读取服务端明细

- 日期：2026-09-15
- 目标：避免网络较慢时用户连续点击保存产生重复账目，并让创建成功后的页面明细与服务端快照重新对齐。
- 实际完成：在 `uiState` 增加保存进行中的状态；后端请求开始后禁用保存和取消按钮并显示“保存中…”，第二次点击只提示“正在保存，请稍候”。创建成功后先把服务端响应转换成网页记录，再重新请求当前账本明细，最后刷新总览摘要。
- 一致性处理：明细重新读取失败不会撤销已经成功的服务端写入，页面保留本次服务端响应并提示稍后重试；只有创建请求本身失败才进入原有 localStorage 回退。
- 验证方式：前端测试从 36 项增加到 37 项，新增重复点击只发送一次 POST 的测试，并更新普通记录和押金测试验证 POST 后 GET 明细；`npm test` 通过 37 项，所有 JavaScript 文件语法检查通过，后端 91 项回归保持通过。
- 遇到的问题：保存函数改成异步后，按钮事件不会自动阻止下一次点击；另外“创建成功但重新读取失败”和“创建请求失败”是两个不同状态，不能共用本地回退逻辑。
- 解决方法：用显式的 `recordSaveInFlight` 状态和按钮禁用控制重复提交；将明细重新读取封装为独立的安全刷新函数，只在创建请求抛错时恢复本地快照。
- 面试表达：我为异步写入增加幂等体验控制，避免重复点击造成重复业务事实；写入成功与后续读取失败分开处理，保留服务端成功结果并给出可操作提示，而不是误把成功写入回滚成本地数据。
- 后续疑问：如何把服务端成功、本地回退、明细刷新失败和重复请求设计成更清晰的页面状态，并开始处理修改、删除和登录权限。

## 第 66 步：区分写入状态与服务端业务冲突

- 日期：2026-09-15
- 目标：让用户清楚知道账目是服务端成功、本地回退、正在保存，还是被服务端业务规则拒绝。
- 实际完成：`uiState` 增加 `recordSaveStatus`；表单提示支持成功、本地、冲突、处理中和普通错误样式。网络失败仍恢复 localStorage 并保存；服务端返回带 `code` 的业务错误时保留表单内容，不写入本地副本；创建成功但明细重新读取失败时保留服务端返回记录并提示稍后重试。
- 用户体验：保存按钮和取消按钮在请求期间禁用；成功、回退、冲突和读回失败使用不同颜色和文案，用户可以据此判断下一步是等待、修正还是重新联网。
- 验证方式：前端测试从 37 项增加到 38 项，新增服务端冲突不回退本地的测试；`npm test` 通过 38 项，所有 JavaScript 文件语法检查通过，后端 91 项回归保持通过。
- 遇到的问题：所有 HTTP 错误都走本地回退会把服务端的押金关联冲突或账户规则拒绝再次写入本地，造成两边状态不一致；重新读取失败也不能当成创建失败处理。
- 解决方法：依据错误对象是否带服务端业务 `code` 分流，只有网络类失败进入本地回退；业务冲突直接展示服务端中文说明并保留输入，读回失败则保留已成功的服务端记录。
- 面试表达：我把异步写入拆成可观察的状态机，并根据稳定错误码区分可重试的网络问题和需要用户修正的业务冲突，避免自动回退制造重复或不一致数据。
- 后续疑问：如何把服务端的 `PUT`、`DELETE` 接到编辑和删除按钮，并处理押金结算链路的锁定和本地回退。

## 第 67 步：把账目修改和删除同步到后端

- 日期：2026-09-15
- 目标：让已有账目的修改和删除与服务端账本保持一致，同时不破坏押金原记录与结算记录的锁定规则。
- 实际完成：客户端新增账目 `PUT` 和 `DELETE` 请求封装；编辑时提交与创建相同的类型、类别、账户、金额、日期和押金字段，成功后用服务端响应更新页面并重新读取明细、总览；删除成功处理服务端 `204` 空响应，再刷新页面数据。
- 一致性处理：服务端返回 `deposit_record_in_use`、`deposit_settlement_locked` 等业务错误时保留表单或删除目标，不绕过规则写本地；网络失败才恢复 localStorage，并用原账目的类型、类别、日期、金额、账户名称和押金对象指纹找到本地记录后回退。
- 验证方式：前端测试从 38 项增加到 40 项，新增服务端账目修改和 204 删除流程；`npm test` 通过 40 项，所有 JavaScript 文件语法检查通过，后端 91 项回归保持通过。
- 遇到的问题：服务端删除接口没有 JSON 响应，客户端如果无条件调用 `response.json()` 会把成功误判为失败；服务端 UUID 也不能直接写回旧的数字 ID 本地数据。
- 解决方法：JSON 请求层先识别 HTTP 204 并返回空值；修改和删除回退前恢复完整本地快照，再按稳定业务字段重新解析本地记录和账户引用。
- 面试表达：我让前端修改和删除复用后端领域校验，针对 204 空响应补充协议层兼容；跨存储回退时不直接复制 UUID，而是用业务指纹重新绑定本地主键，从而同时保证可用性和数据一致性。
- 后续疑问：账本和账户的修改、删除仍在本地流程中，下一步把它们也切换到服务端，再替换固定开发用户为真实登录身份。

## 第 68 步：账本和账户的修改、删除接入后端

- 日期：2026-09-15
- 目标：完成三类核心资源的渐进式写入，让账本、账户和账目在 HTTP 页面上都优先使用服务端，并保留断网时的本地可用性。
- 实际完成：账本修改调用 `PATCH /api/books/{book_id}`、删除调用 `DELETE`；账户修改调用 `PATCH /api/books/{book_id}/accounts/{account_id}`、删除调用 `DELETE`。成功后分别刷新总览，账户删除还重新读取当前账本明细，以反映历史账目的账户引用清空。
- 一致性处理：服务端业务错误码直接显示提示，不自动写本地绕过规则；网络失败先恢复 localStorage，再按账本名称/分类或账户名称/类型/本金的唯一指纹定位本地对象后回退，重复对象无法唯一定位时停止操作。
- 用户体验：账本和账户保存、删除期间按钮会锁定，保存与删除互相排斥；删除服务端返回 204 时按成功处理，明细刷新失败会提示稍后重试而不撤销已完成的服务端删除。
- 验证方式：前端测试从 40 项增加到 46 项，新增账本和账户的 PATCH/DELETE 流程、账本业务冲突和账户网络回退；`npm test` 通过 46 项，所有 JavaScript 文件语法检查通过，后端 91 项回归保持通过。
- 遇到的问题：服务端 UUID 与旧本地数字 ID 不能直接混用；删除账本或账户后，当前页面还可能保留已删除资源的旧明细。
- 解决方法：回退时恢复完整本地快照并按业务字段重新查找；服务端删除成功后主动刷新总览和受影响的账本明细，刷新失败则标记明细需要重试。
- 面试表达：我把三类资源的写入切换拆成统一的成功、冲突和网络回退路径，用 204 状态处理和唯一业务指纹解决跨存储主键差异，并在删除后刷新派生数据避免页面显示旧引用。
- 后续疑问：现在后端仍使用固定本地开发用户，下一步先设计注册、登录、会话过期和用户边界，再让前端根据登录状态读写数据。

## 第 69 步：建立登录、密码哈希和会话边界

- 日期：2026-09-15
- 目标：把固定开发身份之外的真实用户认证基础建起来，为手机和电脑使用同一账号做准备。
- 实际完成：数据库 schema 从 v4 升到 v5；用户表增加密码哈希，会话表保存令牌摘要和过期时间；新增 `POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`。
- 安全处理：密码使用 PBKDF2-HMAC-SHA256 加随机盐保存，数据库不保存明文密码；浏览器只通过 HttpOnly、SameSite Cookie 持有原始会话令牌，数据库保存 SHA-256 摘要，过期会话在读取时清理。
- 跨端口处理：前端请求统一携带 Cookie；后端通过 `JIZHANGBEN_CORS_ORIGINS` 显式列出允许的前端来源并开启凭据传递，默认不放开任意来源。
- 兼容策略：业务接口带有效会话时按会话用户隔离数据；无 Cookie 时暂时回退固定开发用户，带无效 Cookie 则返回 401，等网页登录页接入后再关闭回退。
- 验证方式：新增 5 项认证接口测试，覆盖注册自动登录、当前用户、密码错误、退出、无效 Cookie、重复邮箱和关闭开发回退；认证/迁移相关测试 18 项通过，完整后端回归 97 项，前端 48 项保持通过，并新增登录用户空账本读取测试。
- 遇到的问题：已有 91 项后端测试和旧数据库都依赖 schema v4 与固定开发身份，直接强制登录会让旧页面和迁移测试同时失效。
- 解决方法：用版本化迁移增量增加认证表结构；请求依赖优先识别会话，暂时保留可由 `JIZHANGBEN_ALLOW_DEV_FALLBACK` 关闭的“无 Cookie 才回退”兼容开关，并为无效 Cookie 单独返回 401，避免伪造身份继续使用开发用户。
- 面试表达：我用 PBKDF2 加盐哈希和只存摘要的会话表建立认证基础，再把用户解析集中在请求依赖中；通过兼容开关分阶段替换固定身份，避免认证迁移一次性破坏已有数据和接口。
- 后续疑问：网页已经接入登录/注册界面，下一步是在确认真实用户数据可迁移和可恢复后关闭开发用户回退。

## 第 70 步：统一处理会话过期

- 日期：2026-09-15
- 目标：当登录 Cookie 过期或被服务端撤销时，让网页立即退出服务端数据状态，并给用户明确的重新登录提示。
- 实际完成：请求层保留 HTTP 状态码；受保护接口返回 401 时调用统一处理函数，清除当前用户和认证状态，恢复启动时的 localStorage 快照，刷新页面状态并显示“登录状态已过期，请重新登录”。登录、注册、当前用户和退出接口自己的 401 仍留在认证表单中处理，避免把密码错误误判成会话过期。
- 后端验证：把测试会话的 `expires_at` 改为过去时间后访问业务接口，确认返回 `authentication_required`，并确认过期会话记录已经从数据库清理。
- 前端验证：模拟受保护接口返回 401，确认页面状态变为 `guest`、用户信息被清空、本地快照恢复、登录过期提示出现。
- 遇到的问题：读请求失败原本只会静默回退本地，页面可能仍显示“已登录”；同时认证接口自己的 401（密码错误）不能触发全局退出逻辑。
- 解决方法：在请求层按路径排除 `/auth/*`，只对受保护接口的 401 通知认证模块；认证模块再统一清理会话状态和恢复本地数据，避免每个业务表单各写一份过期处理。
- 面试表达：我把 401 当作跨模块的会话状态变化处理，而不是普通网络异常；请求层统一发出事件，认证模块负责清理状态和恢复快照，既保证用户提示一致，也避免密码错误触发误退出。
- 后续疑问：如何在完成迁移演练、备份恢复和网页登录验收后，把 `JIZHANGBEN_ALLOW_DEV_FALLBACK` 默认关闭。

## 第 71 步：补齐登录失败和退出后的页面状态

- 日期：2026-09-16
- 目标：让登录用户读取服务端失败和主动退出时都有可见、可恢复的页面状态。
- 实际完成：新增顶部数据通知，将“已登录但服务端账本暂时无法读取”从隐藏的登录面板提升到页面可见区域；读取失败时保留认证用户和登录状态，同时回退到浏览器本地数据；退出后清除用户、恢复本地快照、显示未登录按钮，并提示当前使用本地数据。
- 验证方式：前端新增登录数据读取失败和退出状态测试，`npm test` 通过 50 项；所有 JavaScript 文件语法检查通过，后端 97 项回归保持通过。
- 遇到的问题：登录面板在成功后会关闭，原有失败提示只写在面板内；退出后如果没有状态提示，用户无法判断页面数据来源。
- 解决方法：把认证错误和数据来源提示分开，认证失败仍显示在登录表单，数据读取失败和退出结果写入顶部 `startupNotice`；登录用户读取失败不清除认证身份，只有明确的 401 才触发会话过期处理。
- 面试表达：我区分“认证成功但数据读取失败”和“认证失败”两个状态，避免把网络故障误判成登录失败；退出时同时清理服务端状态标记和本地页面快照，让数据来源对用户可见。
- 后续疑问：如何用脱敏备份演练真实登录用户的数据迁移和恢复，并验证失败事务不会破坏原数据。

## 第 72 步：用真实登录会话演练备份迁移往返

- 日期：2026-09-16
- 目标：在关闭开发用户回退前，用真实注册用户完成一次脱敏备份的预览、导入、导出和对账。
- 实际完成：测试先注册独立用户，再用该会话调用 `preview-local` 和 `import-local`；导入后读取账本、账目、总览和服务端导出，比较预览数量、收入、支出、净资产与导入结果，并检查押金退回记录仍指向重建后的原押金 ID。最后退出登录，确认开发用户看不到刚才用户的账本。
- 验证方式：新增 1 项真实会话迁移往返测试，后端完整回归通过 98 项；前端 50 项和 JavaScript 语法检查保持通过。
- 遇到的问题：之前的迁移测试调用接口时没有复用 Cookie，主要验证的是固定开发用户回退，不能证明真实登录用户的数据边界。
- 解决方法：在测试中复用同一个 `AsyncClient` 保存注册后的会话 Cookie，并在退出后再次请求账本列表，验证会话用户和开发用户的结果分离。
- 面试表达：我把迁移验收拆成只读预览、事务导入、导出回读和退出隔离四个阶段，用同一登录会话串起真实用户链路，再用收支和净资产对账证明数据没有只“导入数量”而丢失业务含义。
- 后续疑问：如何增加重复导入和恢复失败演练，并在所有接口都强制登录后再做跨设备联调。

## 第 73 步：关闭默认开发用户回退

- 日期：2026-09-16
- 目标：让生产环境的业务接口不再因为缺少 Cookie 而使用固定开发用户，所有真实数据请求都必须经过登录会话。
- 实际完成：应用将 `JIZHANGBEN_ALLOW_DEV_FALLBACK` 默认值从 `1` 改为 `0`；没有 Cookie 的账本、选项、账目、总览和备份请求统一返回 `401 authentication_required`。旧接口测试通过 `backend/tests/conftest.py` 显式打开兼容开关，不把测试便利混入生产默认值。
- 验证方式：新增覆盖账本、类别、记账类型、总览和备份读取的默认强制登录测试；后端完整回归 99 项，前端 50 项保持通过。
- 遇到的问题：历史后端测试大量使用无 Cookie 的开发用户请求，如果直接改默认值会让测试全部变成认证失败，无法区分旧测试兼容问题和业务代码问题。
- 解决方法：把兼容开关集中到测试配置，只在测试进程显式设置为 `1`；另加一条删除环境变量后的默认安全测试，确认应用本身默认拒绝无会话请求。
- 面试表达：我把开发便利配置和生产安全默认值分离，应用默认拒绝匿名业务请求，测试通过显式 fixture 保持旧场景可回归，同时用独立测试验证默认配置确实会返回 401。
- 后续疑问：如何在两个浏览器中登录同一账号，验证账本、账户、账目和备份读取的一致性，再开始部署配置。

## 第 74 步：双客户端验证跨设备数据一致性

- 日期：2026-09-16
- 目标：模拟手机和电脑两个独立浏览器，确认同一账号的数据可以共享，不同账号无法读取对方账本。
- 实际完成：测试创建三个独立客户端。手机客户端注册用户并创建账本、账户、自定义类别、记账类型和一笔支出；电脑客户端用同一邮箱登录，读取到相同账本、账户、账目和总览；第三个客户端注册另一用户，读取账本为空，访问第一个用户的账本账户返回 404。
- 验证方式：新增双客户端会话隔离测试，后端完整回归通过 100 项；前端 50 项和 JavaScript 语法检查保持通过。
- 遇到的问题：服务端账本和账户响应还包含用户 ID、创建时间等字段，不能把完整响应对象直接与简化预期比较，否则会把协议元数据差异误判成跨设备失败。
- 解决方法：测试保留完整响应用于接口契约检查，只对 ID、名称、本金、记录内容和总览金额等关键业务字段断言；两个客户端使用独立 Cookie 容器，第三个客户端单独验证用户边界。
- 面试表达：我用独立会话模拟多设备，而不是只在同一个客户端重复请求；一端写入、另一端读取，再加入第三个用户做越权对照，分别验证共享数据和隔离边界。
- 后续疑问：如何把这条联调加入持续集成，并在真实 HTTP 端口下检查 Cookie、CORS 和手机浏览器行为。

## 第 75 步：建立 GitHub Actions 持续集成

- 日期：2026-09-16
- 目标：让每次提交和合并请求都在干净环境中自动验证前端、后端和 JavaScript 语法。
- 实际完成：新增 `.github/workflows/ci.yml`，使用 Ubuntu、Node.js 22 和 Python 3.12；先运行 `npm test`，再逐个执行 `node --check`，最后安装 `backend/requirements.txt` 并运行后端测试。工作流只申请读取代码权限，覆盖 `main` 推送和所有合并请求。
- 验证方式：本地前端 50 项、后端 100 项和语法检查全部通过；工作流命令与本地测试命令保持一致，后续上传 GitHub 后由 Actions 在全新环境实际执行。
- 遇到的问题：项目没有 `package-lock.json`，CI 不能使用 `npm ci`；同时 JavaScript 文件较多，直接把通配符传给 `node --check` 在不同 shell 中行为不一致。
- 解决方法：前端没有第三方依赖时直接运行 `npm test`，语法检查用 Bash 循环逐文件执行；Python 依赖统一从 `requirements.txt` 安装，并启用 pip 缓存减少重复下载。
- 面试表达：我把本地验证命令原样搬进 GitHub Actions，并固定 Node/Python 版本和依赖文件；这样能同时发现代码回归、语法问题和“本机装过依赖所以能跑”的环境差异。
- 后续疑问：如何加入部署配置，在真实 HTTP 端口下验收 Cookie、CORS 和手机浏览器访问。

## 第 76 步：修复 CI 的 Node.js 版本兼容性

- 日期：2026-09-16
- 目标：修复 GitHub Actions 中前端测试退出码 9 的问题，让 CI 使用与测试参数兼容的 Node.js 版本。
- 实际完成：从第一次失败运行日志确认错误发生在 `Run frontend tests`，而不是后端依赖或语法检查；先把工作流从 Node.js 20 升到 Node.js 22。第二次运行仍退出码 9，进一步确认 Node.js 22 对应的参数名是 `--experimental-test-isolation=none`，于是同步修正 `package.json` 测试脚本。
- 验证方式：本机用兼容参数运行前端 50 项全部通过；后端 100 项和 JavaScript 语法检查保持通过；修复提交推送后等待新的 GitHub Actions 运行结果。
- 遇到的问题：本机 Node.js 24 同时兼容新旧参数，因此不能直接复现 Node.js 22 的参数错误；GitHub 邮件只显示“所有任务失败”，必须进入具体 job 日志定位步骤。
- 解决方法：根据 Actions 的失败步骤和退出码，结合 Node.js 22 的 CLI 参数差异，保留 Node.js 22 的长期支持运行时，使用它对应的实验性测试隔离参数，不改变测试隔离语义。
- 面试表达：遇到 CI 连续失败时我先定位失败 job 和具体步骤，再对比不同运行时的命令行兼容性；确认是参数名称差异后只修正测试脚本和文档，保持业务代码不变。
- 后续疑问：如何确认新的 Actions 运行成功，并开始加入部署配置和真实手机浏览器验收。

## 第 77 步：建立单容器同源部署基线

- 日期：2026-09-16
- 目标：让网页和 API 可以通过同一个地址启动，供手机和电脑进行真实 HTTP 验收。
- 实际完成：FastAPI 在检测到 `index.html` 时挂载前端静态目录；新增 `Dockerfile`、`.dockerignore` 和 `docker-compose.yml`，使用 Python 3.12、非 root 用户和 `/data` 持久化卷；容器默认关闭开发用户回退并提供健康检查。
- 验证方式：增加根路径静态入口测试；后端 101 项、前端 50 项和 JavaScript 语法检查全部通过，并用本地 Uvicorn 真实 HTTP 端口确认网页和 `/api/health` 都返回 200。当前环境未安装 Docker，因此镜像构建和手机访问留到下一步在有 Docker 的环境中验收。
- 遇到的问题：前端原先只能由独立静态服务器提供，跨端口部署需要额外 CORS；如果数据库写在容器内部，容器重建会丢失数据。
- 解决方法：让 FastAPI 与网页同源提供服务，减少 Cookie 和 CORS 变量；Compose 使用命名卷保存 SQLite，并把静态目录通过环境变量区分本地仓库与容器路径。
- 面试表达：我先把部署边界收敛为单容器同源服务，降低跨域和会话配置复杂度，再用持久化卷隔离应用生命周期和数据生命周期，同时保留健康检查便于平台探活。
- 后续疑问：如何在真实 HTTP 端口启动服务，使用手机和电脑两个浏览器验证登录、押金结算、备份和跨设备一致性。

## 第 78 步：真实 HTTP 端口验证跨设备会话

- 日期：2026-09-16
- 目标：确认同源部署入口下的网页、Cookie 会话、跨设备数据读取、备份导出和用户隔离能够一起工作。
- 实际完成：用 Uvicorn 在本地 8766 端口启动服务，网页入口和 `/api/health` 均返回 200；第一个独立会话注册用户并创建账本、账户，第二个独立会话登录同一账号后读取到相同账本和账户并成功导出备份；第三个账号的账本列表为空，访问第一个账号的账户返回 404。
- 验证方式：使用 PowerShell 的独立 `WebRequestSession` 保存 Cookie，不复用 ASGI 测试客户端；健康状态为 `ok`、schema 版本为 5，同账号 ID 一致、共享账本和账户各 1 个、备份账本 1 个、第三账号账本 0 个、越权状态 404。
- 遇到的问题：单元测试可以证明接口逻辑，却不能证明真实端口上的静态文件、Cookie 保存和同源路径组合正常；只用一个客户端也无法发现会话之间的数据串线。
- 解决方法：启动真实 HTTP 服务后为每个客户端建立独立 Cookie 容器，按“注册写入—另一端登录读取—第三端越权检查”的顺序验收，并在结束后删除联调数据库。
- 面试表达：我把跨设备验收拆成共享数据和隔离边界两组证据，使用独立会话验证同账号一致性，再用第三账号验证不能越权读取。
- 后续疑问：如何在安装 Docker 的环境构建镜像，并用手机浏览器访问电脑局域网地址完成押金、筛选、总览和备份流程验收。

## 第 79 步：Docker 同源部署与手机视口验收

- 日期：2026-09-16
- 目标：在真实 Docker HTTP 入口下启动记账本，并确认手机尺寸页面可以继续使用。
- 实际完成：安装 Docker Desktop 的 WSL2 后端并把程序放在 G 盘；处理 Windows 版本 26200 上 Docker 启动时残留 AF_UNIX 套接字导致的 `error 1920`，隔离旧运行目录、关闭与项目无关的可选 AI 推理服务后恢复引擎。使用 Compose 构建 `jizhangben-app` 镜像并启动 `jizhangben-app-1` 容器，SQLite 使用命名卷持久化。
- 验证方式：`docker compose config` 和 `docker compose up --build -d` 均成功；容器显示 `Up (healthy)`，根路径和 `/api/health` 均返回 200，健康响应为 `{"status":"ok","database":"ok","schema_version":5}`。在真实 HTTP 页面用 320px、375px、768px 三种视口检查记一笔、账户、明细、总览、管理和登录面板，页面宽度没有超过视口，主要控件没有低于 44px，浏览器没有错误或警告。
- 遇到的问题：Docker Desktop 首次启动因 Windows AF_UNIX 套接字残留而崩溃；直接删除套接字返回系统错误，镜像第一次构建还需要较长时间下载 Python 基础层。
- 解决方法：完全退出 Docker 后改名包含残留套接字的父目录并让 Docker 重建空目录，保留旧目录作为隔离备份；关闭 Docker AI 推理服务避免它再次创建同类套接字。构建时保留网络下载过程，不把缓慢下载误判成代码失败。
- 面试表达：我先用健康检查和真实 HTTP 响应证明容器可用，再用多视口验收页面宽度和触控尺寸；遇到宿主机 IPC 文件错误时只隔离运行时目录，不重置 Docker 数据盘，从而同时保护已有数据和恢复部署能力。
- 后续疑问：如何让真实手机通过电脑局域网地址完成注册、登录、押金结算、筛选、总览和备份，并演练一次数据库卷恢复。

## 第 80 步：把登录改成 HTTP 页面入口

- 日期：2026-09-17
- 目标：通过 Docker/HTTP 访问时，用户先完成登录或注册，认证成功后才进入记账本主体。
- 实际完成：新增登录门禁状态；HTTP 页面启动时隐藏账本、账本管理和顶部切换条，只显示登录/注册入口。认证成功后再读取服务端账本并显示主体；退出或会话过期会清空当前页面内存并回到登录入口，但不会删除浏览器本地备份。直接打开 `index.html` 时继续保留 localStorage 本地兼容模式。
- 遇到的问题：原流程会先读取并显示本地账本，再异步检查会话；这会让未登录用户看到不属于当前服务端账号的数据，也不符合“登录完成后才进入”的使用路径。
- 解决方法：将账本主体放入独立的 `ledgerApp` 容器，在认证状态为 guest/loading 时保持隐藏；服务端读取函数只有 authenticated 状态才能执行，401、退出和会话检查失败都进入同一个登录门禁。新增前端测试覆盖未登录隐藏主体、登录显示主体、退出和会话过期回门禁。
- 验证方式：前端自动化测试 51 项和 JavaScript 语法检查通过；随后在 Docker 同源 HTTP 页面检查未登录状态，确认登录卡片可见、账本主体不可见，健康接口继续返回正常状态。
- 面试表达：我把认证状态建模成页面级门禁，先隔离未登录视图和数据读取，再让登录成功成为加载账本的前置条件；会话失效时统一回到门禁，避免把本地兼容数据误当成服务端账号数据。
- 后续疑问：如何用真实手机通过局域网地址注册并登录，再验收同一账号在电脑和手机看到一致的账本数据。

## 第 81 步：完善注册表单的校验和说明

- 日期：2026-09-17
- 目标：让第一次使用的人清楚知道注册需要什么，并在提交前发现常见输入错误。
- 实际完成：注册模式增加“确认密码”字段、密码规则提示和邮箱格式校验；登录模式只保留邮箱和密码，切换模式时同步更新字段显示、自动填充语义和按钮文字。注册成功后仍由后端自动建立 HttpOnly 会话并进入账本。
- 当前注册形式：邮箱 + 显示名称 + 密码 + 确认密码；密码至少 8 个字符，不需要手机号或验证码，同一邮箱不能重复注册。
- 遇到的问题：原表单只有一个密码框，用户输错密码只能等服务端返回；邮箱字段虽然是 `type=email`，但点击普通按钮不会自动阻止不规范地址。
- 解决方法：在前端提交前增加邮箱、密码长度、显示名称长度和两次密码一致性检查；确认密码只用于浏览器校验，不会发送给后端，并让邮箱、密码输入框复用统一宽度和手机触控尺寸。
- 验证方式：前端自动化测试 52 项和 JavaScript 语法检查通过，覆盖注册密码不一致时不发请求以及成功注册请求不包含确认密码。
- 面试表达：我把表单校验前移到客户端，减少无效请求，同时保持后端继续做最终校验；确认密码是交互辅助字段，不进入用户数据模型，避免扩大敏感信息的传输范围。
- 后续疑问：是否需要加入找回密码、邮箱验证和多设备登录管理；这些功能需要邮件服务和更完整的账户安全策略，等核心记账流程稳定后再做。

## 第 82 步：确定邮箱验证的后续边界

- 日期：2026-09-17
- 决策：当前版本不接入邮箱验证码，继续使用邮箱、密码和确认密码完成自助注册；邮箱验证、找回密码和登录限流登记为后续认证增强。
- 理由：项目当前重点是完成可在电脑和手机使用的核心记账流程。验证码会引入邮件服务、令牌过期、重发限流、失败重试和生产密钥管理，暂时会增加主线复杂度。
- 后续方案：准备公开给陌生用户使用前，优先采用一次性、短时有效的邮箱验证链接，再补找回密码和异常登录保护。
- 面试表达：我根据交付阶段区分认证需求，先用低依赖的邮箱密码登录完成产品闭环，再把邮箱所有权验证拆成独立迭代，避免把外部邮件服务的不确定性带入核心功能开发。

## 第 83 步：准备真实手机局域网接入

- 日期：2026-09-17
- 目标：让手机通过与电脑相同的 Wi-Fi 访问 Docker 中的记账本登录入口。
- 实际完成：确认 `jizhangben-app-1` 持续为 `Up (healthy)`，电脑 WLAN 地址为 `10.141.156.100`；从电脑访问 `http://10.141.156.100:8000/` 和 `/api/health` 均返回 200，并在局域网地址页面确认未登录时只显示登录入口。
- 手机操作：手机和电脑连接同一 Wi-Fi 后，在手机浏览器打开 `http://10.141.156.100:8000/`；注册或登录后再开始账本操作。电脑休眠、Docker 停止或更换 Wi-Fi 后，局域网地址可能变化。
- 遇到的问题：`127.0.0.1` 只代表当前设备，手机不能用电脑的 localhost；宿主机能访问局域网地址仍不能替代另一台设备的最终验收。
- 下一步：用真实手机完成注册、登录、创建账本和账户、记一笔、押金结算、筛选、总览和备份，再检查电脑端是否能看到同一账号的数据。

## 第 84 步：确定 Android App 技术路线并建立打包边界

- 日期：2026-09-17
- 目标：把已经能在浏览器和 Docker HTTP 页面运行的记账本，逐步变成可以安装到 Android 手机的 App。
- 决策：采用 Capacitor 复用现有 HTML、CSS 和 JavaScript，不重写成 React Native 或 Flutter。这样可以先学习网页资源如何同步到原生 WebView，再逐步加入相机、通知等手机能力。
- 实际检查：Node.js 24.19.0 和 npm 11.17.0 已存在；电脑当前没有 Android Studio、Android SDK 和 ADB，因此暂时不能生成 APK。
- 当前动作：建立 `docs/APP_PLAN.md`，规定 `mobile/www` 为 App 网页资源目录，并准备一个可重复执行的同步脚本；Android 原生工程留到工具链安装后再创建。
- 关键知识：Capacitor 的 `webDir` 指向待打包的网页目录，`sync` 会把该目录复制进原生工程；Android 开发还需要 Android Studio 和 SDK，真实设备或 API 24 以上模拟器才能运行。
- 遇到的问题：仓库根目录同时包含前端、后端、测试和部署文件，不能直接把整个根目录交给 WebView 打包，否则会把无关文件带入 App。
- 解决方法：单独建立 `mobile/www`，只同步 `index.html`、`css` 和 `js`，并把同步过程写成脚本，避免手动复制造成版本漂移。
- 面试表达：我先识别出网页资源与服务端代码的边界，再选择增量接入 Capacitor；通过独立 `webDir` 和可重复同步脚本控制打包输入，降低后续维护成本。
- 后续疑问：如何把 Android SDK 和 Gradle 缓存放到 G 盘，生成第一个调试 APK，并验证 App 中的本地数据模式和远程登录模式。
