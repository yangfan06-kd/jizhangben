# 记账本项目路线图

## 项目目标

把当前只能在单个浏览器本地使用的记账本，逐步升级为一个可部署、可登录、可在手机和电脑上使用的全栈应用。

## 改造前基线

- 前端：单个 `index.html`，包含 HTML、CSS 和 JavaScript
- 数据：浏览器 `localStorage`
- 用户：无账号和权限体系
- 部署：直接打开本地文件
- 备份：JSON 文件导入和导出

## 当前进度（2026-09-17，Android 调试包与局域网连通性已验收）

- 阶段 1 已完成：模块拆分、状态整理和手机端 320px、375px、768px 正式验收均已完成。
- 阶段 2 的计划项已经完成：存储适配、数据版本迁移、损坏数据保护、备份结构校验和关键计算测试均已落地。
- 前端自动化测试共 55 项，覆盖金额、迁移、押金、账户余额、UUID 账户引用、备份校验、后端读取映射、写入路径、重复提交、冲突提示、失败回滚、账本/账户/账目修改删除、登录状态、会话过期、数据读取失败、退出状态和空账号本地数据迁移保护；后端测试共 102 项，覆盖根路径静态入口、认证初始化和默认账户初始化。
- 阶段 3 已完成 schema v1-v5、账目服务层、本地开发身份、账本和账户 CRUD API、类别和类型 CRUD/归档 API、账目 CRUD API、余额和总览 API、服务端备份 API；阶段 4 已完成登录数据结构、会话 API、网页登录面板、会话过期处理、默认强制登录和双客户端数据隔离验证；阶段 5 已完成 localStorage 备份转换器、脱敏迁移演练、服务端预览校验、网页确认导入流程和跨设备接口联调；阶段 6 已加入 GitHub Actions、单容器 Docker Compose 部署基线，并完成真实 HTTP 同源入口、Cookie 会话、备份、跨用户隔离和 Docker 容器健康检查，手机尺寸视口验收也已完成。Android 调试包已在真实小米手机上完成登录、核心记账、押金结算、对象筛选、总览和服务端备份读取验收；手机通过局域网读取电脑 Docker 健康接口和当前账号账本也已验证；同时补上了空服务端账号登录时的本地数据迁移提示，仍待正式 HTTPS 部署。
- 当前网页已能在 HTTP 环境先显示登录入口，认证成功后再读取服务端账本、账户、类别、记账类型、当前账本明细和总览；退出或会话过期会隐藏账本并回到登录页，未登录时不会显示浏览器本地账本数据，直接打开 `index.html` 仍保留 localStorage 兼容模式。登录后新建账本、账户和账目会优先写服务端，账本、账户和账目修改与删除也优先走服务端，记录保存期间会阻止重复提交，成功后重新读取明细，业务冲突留在表单上提示修正，网络失败才回退到当前会话开始前的快照。后端已提供注册、登录、退出和当前用户接口，注册/登录会幂等补齐内置类别和记账类型，新建账本会补齐 7 个默认账户，生产 Docker 默认关闭开发用户回退。手机端 320px、375px、768px 的 HTTP 页面布局和主要页面均已通过，跨设备数据共享已由独立会话验证；Android 调试包已进一步验收对象筛选、总览和服务端备份读取，下一步是局域网访问和跨端核对。

## 分阶段计划

### 阶段 1：前端工程化

- 保留现有功能和数据格式
- 拆分 HTML、CSS 和 JavaScript
- 按功能拆分状态、存储、记账、账户、账本和统计模块
- 统一错误处理和数据校验
- 优化手机端 320px、375px、768px 宽度下的布局

### 阶段 2：本地数据层稳定化

- 抽象存储接口，避免业务代码直接依赖 `localStorage`
- 增加数据版本号和迁移机制
- 增加导入文件的完整结构校验
- 防止损坏数据导致页面无法启动
- 为关键计算逻辑增加测试

### 阶段 3：后端和数据库

- 增加 Python FastAPI 后端
- 使用 SQLite 保存开发环境数据
- 用版本化迁移建立用户和账本表，并验证唯一约束、外键与级联删除
- 建立账户表，限制资产/负债类型、整数分本金和账本归属
- 建立类别和记账类型表，支持内置、自定义、归档和稳定行为代码
- 建立账目表，保存整数分、日期、账户流向、类型、类别和押金关联
- 建立账目服务层，在同一事务中校验跨用户、跨账本和跨押金记录关系
- 提供 `POST /api/books/{book_id}/records`，统一返回 201、404、409 和 422 响应
- 应用启动时幂等建立本地开发用户，并提供 `GET /api/books`、`POST /api/books`
- 提供 `GET /api/books/{book_id}/accounts`、`POST /api/books/{book_id}/accounts`
- 提供 `GET /api/categories`、`GET /api/record-types`，只返回当前用户的未归档选项
- 提供 `POST /api/categories`、`POST /api/record-types`，创建自定义选项并校验名称、代码和行为
- 提供 `GET /api/books/{book_id}/records`，支持日期、账户、类别和关键词筛选
- 提供 `DELETE /api/books/{book_id}/records/{record_id}`，保护已参与押金结算的原记录
- 提供 `PUT /api/books/{book_id}/records/{record_id}`，复用创建校验并锁定已结算押金记录
- 提供 `PATCH /api/books/{book_id}/accounts/{account_id}`、`DELETE /api/books/{book_id}/accounts/{account_id}`
- 提供 `PATCH /api/books/{book_id}`、`DELETE /api/books/{book_id}`，同名时返回提示，删除时级联清理账本内账户和账目
- 提供类别和记账类型的修改、归档 API；系统内置项及历史记录的 `behavior` 不可随意改动
- 实现账户余额、各账本及总览的收入、支出和净资产计算，并用押金全退、少退和分次退回场景与现有前端对账
- 提供 `GET /api/overview`，统一返回账户余额、期间收支、负债和全部账本净资产合计
- 提供 `GET /api/backups/export`、`POST /api/backups/import`，只处理当前用户数据，导入时生成新 ID 并恢复跨表关联
- 固定接口清单和错误格式，为网页接入后端提供稳定契约

### 阶段 4：登录和权限

- 注册、登录和退出
- 密码安全存储
- 用户只能访问自己的账本和账目
- 登录状态过期和错误提示
- 后端统一校验所有输入
- 在每个读写接口验证真实登录身份，停止用固定开发用户代表公开访问者
- 覆盖未登录、跨用户、重复请求和常见输入错误的自动化测试
- 已完成第一步：schema v5 增加密码哈希和会话表，新增注册、登录、退出、当前用户 API，网页增加登录/注册面板，并验证无效会话返回 401
- 已完成第二步：后端清理过期会话并返回统一 401；前端收到受保护接口的 401 后清除登录状态、恢复本地兼容数据并提示重新登录
- 已完成第三步：关闭默认的无 Cookie 开发用户回退；只有显式设置 `JIZHANGBEN_ALLOW_DEV_FALLBACK=1` 时才允许旧接口测试使用固定开发用户，生产默认必须登录
- 后续增强：接入邮箱验证链接、找回密码和登录失败限流；需要邮件服务、令牌过期和重发策略，暂不阻塞当前局域网可用版本

### 阶段 5：网页接入后端与旧数据迁移

- 在现有网页中建立统一的数据访问层，使账本、账户、类别、类型和账目逐步改用后端 API
- 已完成第一步：账本和账户读取支持后端 API、UUID 映射、HTTP 页面和 `file://` 页面差异处理，以及请求失败回退
- 已完成第二步：类别、记账类型、当前账本明细和总览读取支持后端 API；响应映射与本地快照采用整体切换，避免半迁移状态
- 已完成第三步：增加 localStorage 备份到服务端备份的离线转换器，并用脱敏数据验证旧 ID 作用域、转账与押金关联、统计对账和失败回滚
- 已完成第四步：增加 `/api/backups/preview` 和 `/api/backups/preview-local`，在不写数据库的情况下返回迁移数量、分账本摘要和收支净资产对账结果
- 已完成第五步：网页备份区域增加迁移预览卡片和确认导入流程，确认后调用 `/api/backups/import-local`，预览取消或失败不会清除本地数据
- 已完成第六步：新建账本和账户优先调用后端 API，成功使用服务端 UUID，网络失败时恢复本地快照并继续本地保存
- 已完成第七步：新建账目优先调用后端 API，保留类型、类别和账户 UUID 映射；普通账目、转账和押金方向使用统一请求契约，失败时恢复本地账户引用再保存
- 已完成第八步：保存期间锁定按钮防止重复提交，服务端成功后重新读取当前账本明细并刷新总览
- 已完成第九步：区分服务端业务冲突、网络失败和明细刷新失败，分别提示修正、本地回退或稍后重试
- 已完成第十步：账目修改使用服务端 `PUT`、删除使用服务端 `DELETE`；网络失败时按业务指纹回退本地，押金原记录和结算记录的服务端锁定继续生效
- 已完成第十一步：账本和账户的修改、删除使用服务端 `PATCH`/`DELETE`，成功后刷新关联数据，网络失败回退本地，服务端业务错误不绕过
- 保留浏览器本地数据和原有备份，先做独立数据副本联调，再迁移真实数据
- 提供服务端 JSON 导出、校验和事务式导入；建立旧 ID 到新 ID 的映射，补齐转账与押金关联
- 迁移前后对比账本数、账户数、账目数、收支、押金结算和净资产；任何不一致都停止切换
- 明确切换和回退步骤，在服务端数据确认可读且备份可恢复前不清除 `localStorage`

### 阶段 6：手机可用、部署和质量保障

- 前后端部署到线上环境
- 先验收手机浏览器使用；桌面与手机登录同一账号后能看到相同数据，无需先制作原生 App
- 已确定 App 路线：使用 Capacitor 复用现有网页，先建立 `mobile/www` 打包基线，再准备 Android Studio、SDK 和真实设备生成 APK；具体边界见 [`docs/APP_PLAN.md`](docs/APP_PLAN.md)
- 已完成 App 第一个真机构建闭环：Android Studio、JDK 21、SDK 和 Gradle 缓存放在 `G:\project\android-tools`，已创建 `android/` 原生工程、生成调试 APK，并在真实 Android 手机上安装启动；已完成筛选、总览和服务端备份读取验收，下一步是局域网访问和跨端核对
- 配置生产环境数据库、备份、恢复和监控；演练一次实际恢复
- 增加自动测试和 GitHub Actions
- 已在真实手机上验收注册登录、建账本、建账户、普通记账、押金结算、对象筛选、总览和服务端备份读取；新增空账号登录时本地数据保护和迁移提示
- 补充项目架构、接口和部署文档
- 上线前关闭固定开发身份，并检查 HTTPS、登录状态和用户数据隔离

## 第一版可用目标

完成阶段 6 后，一个用户可以在手机和电脑浏览器登录同一账号，创建账本和账户、记账及处理押金、查看一致的统计结果，并导出和恢复备份。手机浏览器先作为跨设备验收入口；随后用 Capacitor 生成 Android APK，让同一套网页代码可以安装到手机上。原生 App 的同步和发布准备按 [`docs/APP_PLAN.md`](docs/APP_PLAN.md) 分阶段进行。

## 接下来三个学习步骤

1. 在手机浏览器通过局域网同源地址验收登录、押金、筛选、总览和备份流程。
2. 用同一账号在电脑浏览器、手机浏览器和 APK 间核对账本、明细、账户余额和净资产。
3. 配置正式 HTTPS 后端，让 APK 摆脱 USB 转发，再处理签名 APK/AAB 和发布准备。

## 后续认证增强清单

- 在核心记账流程稳定后，再接入邮箱验证链接，确认注册者拥有该邮箱。
- 增加找回密码流程，使用一次性、短时有效的重置令牌。
- 增加登录失败和验证码重发限流，并统一处理账号枚举风险。
- 配置邮件服务密钥、发件域名和失败重试后，再开放陌生用户注册。

## 阶段 1 验收标准（已完成）

- 现有记账、账户、账本、筛选、统计和备份功能继续可用
- 页面在手机浏览器中没有横向滚动
- 数据读写集中在明确的存储模块中
- 关键输入错误不会导致页面崩溃
- 能够在不改变现有用户数据的情况下启动和升级

## 开发原则

- 每个阶段都保持项目可运行
- 先保护现有数据，再进行结构调整
- 每次修改保持小范围、可回退
- 先写清楚数据结构和边界，再增加功能
- 功能完成后补充测试和文档
